# 0538pay 项目规范

本项目目标：**1:1 精致还原彩虹易支付（epay）到 0538pay**。宁可慢工出细活，不允许攒问题、堆返工。

参考源码位置：`f:/phpstudy_pro/WWW/epay.test`（彩虹易支付 epay 原始 PHP 系统，作为功能与业务的权威对照基准）。

> **每次打开或新建 Claude Code 窗口，都必须先读并遵守本规范。** 本文件每个会话自动加载、优先级最高，下面的强制交付门槛对所有会话一律生效，不因换了窗口而重置。

## 开工仪式（每个新窗口/新会话动手前必须先走完，不可跳过）

> **触发词：用户发「开工」（或「新窗口」「开始」）时，立即执行本仪式，无需进一步确认。** 新窗口/新会话即使用户没喊，动手前也必须先走完本仪式。

在开始任何任务、写任何代码之前，**先按序完成下面三步，并向用户简短复述后再等派活**：

1. **读并遵守本规范 `CLAUDE.md` 全文**，重点吃透：1:1 精仿 epay 的目标、参考源码位置 `epay.test`、下面三道强制交付门槛（关卡一 epay 完整功能对比 / 关卡二 自检修复 / 关卡三 回写进度）。
2. **读记忆索引 `MEMORY.md` 及其中相关记忆文件**，尤其：
   - `dev-progress-master` → `docs/开发进度总览.txt`（判断「做到哪/下一步」先读它）
   - `epay-feature-parity-checklist` → `docs/epay功能对比清单.txt`（精仿完整度唯一权威）
   - `self-check-after-feature`（每写完功能必走的自检流程）
   - `admin-ui-design-system`、`css-tokens-are-oklch`（UI 与令牌规范）
   - `progress-docs-two-axis-rule`（回写两轴分工铁律）
   - `epay-onboarding-subchannel-mechanism-2026-07-30`（★若本次任务涉及**服务商/直连通道进件、子商户号、子通道、占位符**，动手前必须先完整读 `docs/0730-商户进件路线规划-不走二清.txt` 全文，并按下面「服务商通道进件开发规则」走）
3. **向用户复述关键约束 3~5 条**（确认已理解交付门槛与两轴回写），然后再接任务。

复述完成前不动手。任务进行中若跨越多步，按需回查上述记忆与文档。

## 强制交付门槛（每次写完代码必须走完，全绿才算完成）

一个功能/页面只要动了代码，交付前**必须**按序走完下面两道关卡。任一不通过 = 未完成，当场修复后重跑，循环到干净。不要等用户来指问题。

### 关卡一：与 epay 完整功能对比

写功能时和交付前，都要和 epay 做**完整**功能对比，而不是只看表面：

1. 找到 epay 中对应的源码（`epay.test/admin/*.php`、`epay.test/user/*.php`、插件、模板等），逐项比对。
2. 对齐清单：
   - **字段**：数据字段名、含义、类型是否一致，有无遗漏。
   - **状态字典**：订单/结算/审核等状态的取值与流转是否一致。
   - **操作项**：列表行操作、批量操作、表单动作是否齐全。
   - **业务规则**：金额计算、费率、风控、校验、边界处理是否与 epay 行为一致。
   - **交互流程**：完整走一遍用户路径，确认与 epay 语义一致，mock 数据业务自洽。
3. 发现缺失或偏差 → 补齐/修正后再进入关卡二。

### 关卡二：自检与修复

按序走完，全绿再交付（详见记忆 `self-check-after-feature`）：

1. **构建**：`cd admin-web && npm run build`（vue-tsc 严格 noUnusedLocals，未用 import/type 删干净）。
2. **对照设计规范**：逐条比 UI 设计规范（内容卡片禁 border/ring/shadow/圆角，用浅灰底 `bg-muted/40` 靠色差区分；表格默认左对齐；金额 `¥` 弱化 + `tabular-nums`；用到的颜色类确认已在 style.css 注册；列表 Panel 带主标题 title 不用 flush；开关用 `<Switch>` 组件）。
3. **对照 epay 真实业务**：即关卡一的结果落到代码，字段/状态/操作与 epay 一致。
4. **交互与边界**：分页 clamp、筛选后选中残留清空、菜单关闭、行操作菜单方向自适应、带图标输入框 `!pl-9`。
5. **真跑一遍**：`npm run dev` 起服务，`curl` 目标路由 + .vue/.ts 模块确认 HTTP 200 且编译产物无 error、dev log 无报错。检查完停掉临时 server。

发现问题 → 当场改 → 重新构建验证 → 循环到干净。

### 关卡三：回写进度文档（收尾，不可跳过）

两道关卡全绿后，**必须**按下面两轴分工回写留痕，才算真正完成。不管新窗口还是老窗口，只要完成了功能就必须回写。没回写 = 未完成。

两个进度文档各管一轴，不要混写（这是刻意分工，别把功能明细塞回总览）：

1. **功能完成度 → `docs/epay功能对比清单.txt`（每次做完功能都要更新这里）**
   - 找到对应功能项，更新状态勾：⬜未做 / 🎨仅前端mock / ✅前后端真接口打通 / ➖epay无我们独有。
   - 这是「某功能精仿到几成」的唯一权威。逐项打勾只在这个文件。

2. **阶段进度 / 下一步 → `docs/开发进度总览.txt`（仅阶段状态变化时才更新）**
   - 只有当一个阶段（如路线图阶段 A~E 的某阶段）推进或完成、或开了新方向时，才回这里更新阶段勾和「下一步」。
   - 顶部「精仿完整度快照」若某大板块整体状态变了（如收单主链从全⬜变基本打通），同步改那几行文字。
   - 总览只打阶段级的勾，不打功能级的勾——功能明细一律看对比清单。

一句话：做完功能先去对比清单打勾；只有阶段性节点才动总览。两边各一套勾，不交叉。

## 本地开发启动

本地跑前后端的标准姿势（前后端分离，两个都要开着）：

- **后端（Go API，端口 8080）**：`cd 0538pay-api && go build -o bin/epvia-server.exe ./cmd/server && ./bin/epvia-server.exe`。**不要用 `go run`**——它每次编译到临时目录的新文件，防火墙当陌生程序反复弹窗；固定产物 + 已改监听回环地址（`configs/config.yaml` 的 `127.0.0.1:8080`）后防火墙不再弹窗。
- **前端（vite，端口 5173）**：`cd admin-web && npm run dev`。
- **访问入口是 `http://localhost:5173`**（前端页面），它把 `/api`、`/uploads` 代理转发到后端 `127.0.0.1:8080`。不要直接开 `127.0.0.1:8080`，那只是纯接口、没有页面。
- **例外**：要用手机 / 局域网别的机器测真实支付回调时，把 `addr` 改回 `":8080"`（绑所有网卡）并给防火墙放行一次，测完可改回回环。

## Python 环境（强制）

本机 `python3` / `python` 在 PATH 里跑不通（无输出、退出码异常）。**凡是需要执行 Python 的场合（跑脚本、技能同步、任何 python 命令），一律显式使用 `E:\Python\python.exe`（Git Bash 下写 `"E:/Python/python.exe"`），不要用裸的 `python3` / `python`。** 该路径为 Python 3.12.10，pip 走 `"E:/Python/python.exe" -m pip`。

## 技术债与优化

不阻塞当前交付但需要后期统一处理的项，记到 `docs/优化待办.txt`，不要就地展开。

## 收工仪式（每个任务交付后 / 结束工作前必须走完，不可跳过）

> **触发词：用户发「收工」（或「换窗口」「新窗口继续」「结束」）时，立即执行本仪式，无需进一步确认。**

**触发时机**：一个功能/任务做完、用户发「收工」、或**用户说要换新窗口/开新窗口/切窗口继续**时，立即开始走本收工仪式——把当前窗口的活收干净、留痕落盘，再为新窗口准备交接。用户一提「收工」或「换窗口」就等于下达了收工指令，不要等进一步确认。

注意：「收工」触发本仪式（收当前窗口的活）；「开工」触发上面的开工仪式（读规范与记忆、复述、接活）。两个词各管一端，别混。

**必须**按序确认下面每一项，全绿才算真正结束。任一没做 = 未收工，补做后再收：

1. **三关卡确认全绿**：关卡一（epay 完整功能对比，缺失已补齐）、关卡二（构建 vue-tsc / go build 无 error + 设计规范 + 真跑 HTTP 200 无报错 + 停掉临时 server）、关卡三（两轴回写：功能明细进 `docs/epay功能对比清单.txt`，阶段变化才进 `docs/开发进度总览.txt`）。任一没过，当场修复重跑到干净。
2. **回写进度留痕已落盘**：确认对比清单对应功能项状态勾已更新（⬜/🎨/✅/➖），若推进了阶段节点则总览也已同步。没回写视为未完成。
3. **技术债归档**：本次发现但未就地解决的项，已记入 `docs/优化待办.txt`，不留在脑子里。
4. **更新记忆**：本次若产生了新的项目事实/用户偏好/工作反馈（非代码可自证的信息），写进 `MEMORY.md` 对应记忆文件；已失效的记忆及时更正或删除。
5. **清理现场**：删掉验证用的临时文件/临时 server/调试产物，确认 `git status` 无意外残留。
6. **全库乱码自检**：若本次动过数据库写入/手工塞过数据/跑过迁移脚本，收工前跑一遍 `bash 0538pay-api/scripts/scan_mojibake.sh`，确认「全库无坏数据 ✅ + 字符集全 utf8mb4 ✅」全绿。发现坏字节（`����`/U+FFFD）当场覆盖重写修掉（原文已损坏无法还原，按同类正常文案补）。成因与说明见记忆 `db-mojibake-scan-and-utf8mb4`。
7. **向用户交付小结**：用几句话说清「做了什么、验证到什么程度（构建/测试/真跑结果如实报，失败就说失败）、下一步建议」，不夸大、不隐瞒跳过的步骤。
8. **为新窗口准备交接（换窗口时必做）**：若本次收工是因为要换新窗口，除上面 1~7 外，还要确保下个窗口能无缝接手——把「当前做到哪、下一步该做什么、有无半成品/未提交改动/待办阻塞」写进 `docs/开发进度总览.txt` 的「下一步」区（阶段级）与对应记忆，让新窗口走完开工仪式读到的就是最新交接点。有未完成的半拉子活，明确写清它的状态和续做入口，不留口头交接。

一句话：开工先读规范与记忆并复述；干活走三关卡；收工（含「换窗口」触发）确认三关卡全绿 + 回写 + 归档 + 更新记忆 + 清理 + 乱码自检 + 如实小结 + 交接留痕。整套闭环每个窗口一律照走。

---

## 代理进件开发规则（仅针对「代理进件平台」这条线，独立于上面的 epay 精仿规则）

> 代理进件是本项目的【自研扩展】，epay 原生没有此功能，**不纳入** epay 精仿对比清单与总览的打勾体系。
> 它的规划、进度、优化、错误都单独在 `docs-代理进件/` 目录下自成一套，与 epay 精仿线并行、互不混写。
> 只有在做代理进件相关任务时才走本节规则；做 epay 精仿仍走上面的三关卡与开/收工仪式。

### 一、写完功能：自检自验，全绿才通过
代理进件的每个功能/页面写完，交付前必须自检自验，全绿才算通过（任一不过当场修复重跑，循环到干净）：
- **构建**：`cd admin-web && npm run build`（vue-tsc 无 error）+ `cd 0538pay-api && go build ./...`（无 error）。
- **对照设计规范**：遵守后台 UI 设计规范（同 epay 线那套：直角无边框卡片/Panel/.tbl/Badge/oklch 令牌/Switch 组件等）；代理端 /agent 与公开页 /enroll 也守同一套视觉规范。
- **对照代理进件文档**：字段/状态机/流程/资金路径/退款规则与 `docs-代理进件/00、01…` 号文档一致，与微信官方进件接口字段对齐（自研模块以「文档 + 微信官方接口」为权威，不看 epay）。
- **交互与边界**：付费前置流程、链接生命周期（终态事件锚定 24h）、退款四道拦截 + 已开通硬锁、数据隔离（代理只看/只退自己名下）等边界都要真校验。
- **真跑一遍**：起前后端，`curl` + 页面确认目标路由 HTTP 200、编译产物无 error、dev log 无报错；一期微信侧先走沙箱。检查完停掉临时 server。

### 二、完工代理进件：做记忆 + 存工作进度
一个代理进件功能/任务做完（收工时），除自检全绿外，必须：
1. **更新记忆**：本次产生的代理进件相关事实/决策/进度，写进 `MEMORY.md` 对应记忆文件（记忆命名/描述里带「代理进件」便于后续检索），已失效的更正或删除。
2. **存工作进度**：把「当前做到哪、下一步做什么、有无半成品/待办阻塞」写进代理进件专属进度文档 `docs-代理进件/开发进度.txt`（没有则新建），让新窗口能无缝接手。**代理进件的进度只写这里，不写 epay 线的 `docs/开发进度总览.txt`。**

### 三、开工代理进件：新窗口先读三样 + 过一遍微信支付技能
新窗口/新会话要做代理进件任务时，动手前必须先读并遵守（读完向用户简短复述再接活）：
1. 本 CLAUDE.md 的「代理进件开发规则」本节。
2. 代理进件相关记忆（`MEMORY.md` 中带「代理进件」的条目及其文件）。
3. 代理进件相关文档（`docs-代理进件/` 下 00、01… 号规划设计文档 + `开发进度.txt` 交接点）。
4. ★换新窗口后，必须先调用一遍技能 `/wechatpay-payment-integration`（微信支付集成技能），
   再动手做代理进件任务。这条每个新窗口强制走一遍，不可跳过。

### 四、两个专属登记文件（代理进件线独立，不与 epay 线的优化待办混写）
- **`docs-代理进件/待优化.txt`**：写代理进件时发现的、不阻塞当前交付但需后期统一优化/打磨的项，就地登记不展开。
- **`docs-代理进件/代理进件错误.txt`**：写代理进件时在自检/真跑中遇到的错误与其修复过程/结论，留痕便于复盘与避免重犯。

★所有登记文件（含 `开发进度.txt`）记录格式要求：字段各占一行、条与条之间空行分隔，该换行换行、该空行空行，绝不堆叠在一起分不清。每条按文件顶部给出的字段模板逐行填写。时间字段统一格式 `2026-07-28 00:00分`（精确到分）。

一句话（代理进件线）：开工先读本节规则 + 代理进件记忆 + `docs-代理进件/` 文档并复述；写完自检自验全绿；完工更新记忆 + 回写 `docs-代理进件/开发进度.txt`；优化项进 `待优化.txt`、错误进 `代理进件错误.txt`。

---

## 服务商通道进件开发规则（epay 精仿线内的合规主线，与上面「代理进件」是两条线，别混）

> 这条线指 **epay 服务商/直连通道的商户进件**：让每个商户在平台服务商名下进件、拿到自己的子商户号（sub_mchid / fy_mchnt_cd），钱由上游**直清到商户自己的号**，平台不碰本金。
> **本平台只走商户进件，坚决不走二清**（钱先进平台账户再转出下游 = 二清 = 红线，我方无牌照不做也做不了）。
> 它属 epay 精仿线，走上面的三关卡与开/收工仪式；本节是**加强细则**，不是替代。
> 与「代理进件（自研扩展，走 `docs-代理进件/`）」是两条独立线，思路可借鉴但进度/打勾不混写。

### 一、触发与必读（涉及进件/子商户号/子通道/占位符的任务，开工前强制）
动手前必须先完整读并遵守，读完向用户简短复述再接活：
1. `docs/0730-商户进件路线规划-不走二清.txt` **全文**（决策/目标架构/epay精仿基准/落地阶段/开发方法论/源码坐标清单）。
2. 记忆 `epay-onboarding-subchannel-mechanism-2026-07-30`。
3. 没读完 0730 + 没按第八部分清单啃过对应 epay 源码，**不进入编码**。

### 二、三道防线（源码优先·遗漏目标=0，详见 0730 第七部分）
1. **开工前**：拿 0730 第八部分「源码坐标清单」逐项打开 epay 源码真读，读懂打勾，并就地更新我方现状（⬜/🔶/✅/➖）。
2. **每写一个点**：当场回 epay 源码「三问」——①字段有没有漏（各渠道子商户号字段名不统一：富友=appmchid、微信服务商=appurl…）②改动在 epay 里还牵连哪些地方（★`order.subchannel>0 ? getSub : get` 判据全站 11+ 处，下单/回调/退款/结算/分账/后台订单都要一致，别只改下单）③失败/占位符缺失/状态未通过的边界怎么处理。答不全不许往下写。
3. **每批次交付前**：另起子代理做「epay 源码 vs 我方实现」反向 diff 查漏，差异**非补即归档**（乙类·待凭证），不留「先放着」。

### 三、红线与验收
- 红线：任何服务商类通道对外收款，必须「每商户子通道 + 各自子商户号」；严禁多商户共用平台单一商户号收款。
- 验收：未进件成功的商户该通道对他不可用、下单被拒（不回退平台号）；进件成功后抓下单报文确认用的是商户自己的号；资金到账主体是商户子商户号、平台账户无资金停留；进件资料敏感字段加密落库、回显脱敏。

一句话（服务商进件线）：涉及进件/子商户号的任务，开工先读 0730 全文 + 啃第八部分清单源码并复述；干活走三道防线（清单→三问→子代理查漏）叠加三关卡；只走商户进件不走二清，遗漏压到 0。
