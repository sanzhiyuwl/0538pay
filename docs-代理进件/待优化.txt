====================================================
代理进件平台 · 待优化
====================================================

建档：2026-07-28

说明：
仅记【代理进件线】写代码时发现、暂不就地展开、需后期统一优化/打磨的项。
与 epay 线的 docs/优化待办.txt 分开，不混写。
每条记录字段之间该换行换行、条与条之间空行分隔，不堆叠。


----------------------------------------------------
记录格式（每条按此，字段各占一行，条间空一行）
----------------------------------------------------

编号：

时间：（格式 2026-07-28 00:00分，精确到分）

标题：

问题：

建议：

状态：（待优化 / 已优化）


====================================================
待优化项（新的记在下面，每条之间空一行）
====================================================

编号：1

时间：2026-07-28 17:10分

标题：微信服务商凭证明文存 pay_config

问题：wx_partner_private_key（商户私钥）、wx_partner_apiv3_key（APIv3密钥）目前明文存在 pay_config.v 字段（与 epay 线 sys_rsa_private 现状一致）。属敏感密钥，明文落库有泄露风险。

建议：后期统一做配置层敏感字段加密存储（读写时透明加解密），private_key/apiv3_key 一并纳入；与 epay 线 sys_rsa_private 一起处理，避免两套方案。

状态：待优化


编号：2

时间：2026-07-28 17:10分

标题：删代理未级联清理钱包/名额流水

问题：ConsoleHandler 删代理走物理 Delete，只删 pay_agent 行，pay_agent_quota（钱包）与 pay_agent_quota_log（名额流水）会残留成孤儿记录。真跑测试删测试代理后即观察到钱包/流水孤儿。

建议：删代理时同事务级联删钱包+流水，或改为停用（status=0）软处理不物理删（名额已售涉及资金，倾向禁止有余额/有流水的代理被物理删，只允许停用）。下批做代理端资金闭环时一并定策略。

状态：待优化


编号：3

时间：2026-07-28 17:40分

标题：进件成功扣名额余额不足时的兜底策略未定

问题：settleOnFinish（路径一，进件成功拿到 sub_mchid 后扣代理 1 名额）目前用 `_ =` 忽略 ChangeQuota 的余额不足错误——因为进件已经成功（微信已开通 sub_mchid），不应因名额不足而回滚这个既成事实。但这样会出现代理名额被扣成"应扣未扣"的账不平。

建议：路径一进件成功前就应保证名额已冻结/预扣（创建进件单或提交微信时预占 1 名额），成功时转正、失败时释放；而不是成功后才扣。下批做付费前置 + 资金闭环（第 4 项退款/资金）时统一设计名额的冻结→消耗→释放三态，替换当前的成功后直扣。

状态：待优化
