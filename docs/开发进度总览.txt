================================================================
0538pay 开发进度总览（MASTER 进度图）
================================================================
用途：全项目单一进度视图。散在各专项文档（收尾清单/官网规划/商户中心/saas规划/
      优化待办）的状态汇总到这里，避免写着写着乱套。本文件是"现在做到哪了 + 下一步做什么"
      的权威索引；细节仍看各专项文档。
维护约定：每完成一项 → 回来更新本文件状态位；开新方向 → 先在本文件登记再动手。

★★ 本文件只有一处打勾：文末「路线图 阶段 A~E」，勾的含义 = 该阶段任务是否完成
   （✅已完成 🔨进行中 ⬜未开始 ⏸暂缓）。
   其它地方一律用文字描述状态，不打勾，避免和路线图的勾混淆。
   「某个功能精仿到几成」的逐项打勾（✅🎨⬜➖）唯一在 docs/epay功能对比清单.txt。
   —— 一句话：总览管"阶段进度"，对比清单管"功能完成度"，两边各一套勾，不交叉。
最后更新：2026-07-21
对照基准：epay.test（彩虹易支付）功能对齐。后端方案已改为【B 方案·独立重写·Go 全栈】
          （不复用 epay PHP，功能对齐但语言/表结构/插件契约自研，规避抄袭风险）。
          选型见 docs/后端技术选型.txt，起步架构见 docs/go后端起步架构.txt。
          ★ 与 epay 的功能逐项对比核对表：docs/epay功能对比清单.txt（判断"精仿到几成"看它）。

----------------------------------------------------------------
〇、总体阶段定位
----------------------------------------------------------------
四端 UI 原型 + 后台功能对齐 epay 均已完成。
【接后端已启动】（2026-07-17）：确定 B 方案·Go 全栈独立重写。后端骨架搭好并实跑，
订单 / 商户两条业务域已端到端打通（真库真接口），后台鉴权闭环完成。
其余业务域仍为 mock，按已验证的样板模式逐域推进。
节奏：先出高保真 UI 原型 → 用户满意 → 接后端（进行中，逐业务域切真接口）。

----------------------------------------------------------------
★ 精仿完整度快照（2026-07-21，逐项明细见 docs/epay功能对比清单.txt）
----------------------------------------------------------------
一句话：界面层 ≈90% 完成，能力层 ≈5% 完成。
  · 界面层：admin 后台 / 商户中心 / 官网 UI 基本全对齐 epay 且超出（多了
    SaaS 控制台 + RBAC + CMS）；但绝大多数页面数据仍是 mock。
  · 能力层：后端只有 登录 + 订单列表 + 商户列表 3 个只读真接口。epay 真正的
    价值（收单/渠道/回调/结算/代付/分账/定时）几乎全是 mock。

七大板块与 epay 的对齐状态（此处只做文字概述，不打勾；逐项打勾见对比清单）：
  1. 对外收单 API + 支付全链路 …… 【阶段A完成】下单→收银台→模拟支付→回调→幂等改单→入账→
                                    流水→商户通知 全链路 mock 真跑通过(A1~A6)；V2 REST 接口族(pay/create/query/refund/refundquery+merchant+transfer)已通(C10)；真实渠道下单待凭证
  2. 支付渠道插件（epay 51 个）…… 【框架通·3渠道落地】mock + 支付宝当面付 + 微信Native(下单待凭证) + 免签epay(V1/MD5,已端到端真跑)；免签渠道首次实现真实下单全链路
  3. 定时任务 cron ………………… 【基本完整】ticker 调度器：notify重试 + 对账 + 超时关单 + 自动结算 + 分账自动执行 + 风控自动关停 均真跑；cronkey 保护手动触发端点(投诉complain卡渠道API归B类)
  4. 管理后台（epay admin 49 文件）… 【后端基本切真】订单/商户/通道/结算/代付/分账/风控/统计/config/仪表盘/公告/导出/清理/SSO 均真接口；文章接CMS；余少量mock(多管理员RBAC我方独有)
  5. 商户中心（epay user 37 文件）… 【基本完整】登录态+查询+提现退款+资料密钥密码+增值项+自助全流程(测试支付/聚合码/域名/邀请返现/使用说明/站内信)已切真接口；实名/充值真实渠道待凭证
  6. 官网 / 前端视觉 …………………… 【前端好】官网+文档+CMS+文章 已成型且真持久化；协议/关于接CMS联动；收银台真二维码；扫码中间页视觉皮肤列⏸(数据链路已覆盖)
  7. 我们独有（epay 无，加分项）…… 【前端好】SaaS 控制台 / RBAC 多管理员 / 官网 CMS，均 mock

唯一关键缺口 = 板块 1/2/3 的收单主链（下单→选通道→插件→收银台→回调验签→改单→
入账→通知商户→结算/代付/分账→定时）。一通，才从"支付管理界面"变成"支付系统"。
补齐顺序见文末阶段 A~E。


================================================================
一、UI 原型阶段（已完成 · 功能明细见 docs/epay功能对比清单.txt）
================================================================
四端 UI 原型全部建好（架构：admin-web 单工程 + 4 套 Layout，路由前缀区分）：
  · 官网        /         SiteLayout      营销门面 + 文档 + 支付结果
  · 管理后台     /admin    AdminLayout     运营端，49 视图（对齐 epay + 收尾13项）
  · SaaS 控制台  /console  ConsoleLayout   平台方多租户管理，5 视图（epay 无，我们独有）
  · 商户中心     /m        MerchantLayout  商户自助端，21 视图
  · 官网内容 CMS（后台「官网管理」组：首页文案 + 开发文档结构化可编辑，epay 无）
四套布局齐全，深浅色主题、响应式、组件同源（Panel/Modal/Drawer/.tbl/Badge/Switch/field-input）。

★ 注意：以上均为前端 UI，数据绝大多数是 mock。「哪个功能做到几成」的逐项完成度，
  统一以 docs/epay功能对比清单.txt 为准（本文件不打功能级的勾）。
  各端设计/规划细节仍见专项文档：收尾开发清单.txt / saas开发规划.txt /
  商户中心开发规划.txt / 官网开发规划.txt / memory: site-content-vs-settings。

原型阶段少数明确取舍（登记备查，不属功能明细）：
  · 投诉管理：暂不做——epay 源码无实体文件，核对后决定不造（避免凭空造功能）。
  · SaaS 控制台 P2 剩余（免密登录分站/下发公告/白标定制/独立域名SSL）：
    优先级低，接后端后再评估（见文末阶段 E2）。


================================================================
二、接后端（Go 全栈，B 方案独立重写）—— 进行中
================================================================
后端工程：0538pay-api/（与 admin-web 平级）。技术栈 Gin+GORM+JWT+shopspring/decimal+Viper。
环境：Go 1.26.5；MySQL 独立库 pay0538 + 账号 pay0538/pay0538（与 epay/wepay 隔离）；
     GOPROXY 必须用国内镜像 goproxy.cn（否则拉不到依赖）。启动方式见 0538pay-api/README.md。

已打通（2026-07-17，阶段勾见文末路线图「接后端轮」开头三项）：
  · 骨架 + 最小闭环：分层 cmd/internal/pkg；统一响应 code=0；AutoMigrate 建表
    （sys_admin/pay_order/pay_merchant）；管理员 admin/admin888；支付插件抽象
    internal/channel（PaymentChannel interface + registry，自研不抄 epay，尚无渠道实现）。
  · 订单域端到端：GET /api/admin/orders（字段对齐 mock/orders.ts；金额补两位、时间格式化
    在 service 层）；Orders.vue 切真接口（src/lib/api 客户端 + vite /api 代理到 :8080）；3 条测试单。
  · 后台鉴权闭环：Login.vue + stores/auth（token 持久化）+ 路由守卫 + 真退出 + 401 全局兜底。
  · 商户域端到端：GET /api/admin/merchants（groupname 派生）；Merchants.vue 切真接口；3 条测试商户。

样板已固化：后端 model→repo→service→handler→route(+seed) + 前端 api 模块→页面切换。
—— 剩余工作（收单主链 / 其余业务域切真接口 / 定时任务）全部编入下方「★ 下一步·路线图」
   的阶段 A~E，不在此平铺。


================================================================
三、技术债 / 优化（详见 优化待办.txt）
================================================================
· #1 路由懒加载 / 代码分割（未做）：主 chunk 已 840KB（gzip 247KB），持续增长。
  放到所有页面开发完后统一改 router 为动态 import。低风险。已编入阶段 E3。


================================================================
★ 下一步 · 路线图（接后端轮，2026-07-17 起，Go 全栈 B 方案）
================================================================
  ✅ 定选型（Go）+ 建后端骨架 + 订单域端到端跑通。
  ✅ 后台鉴权闭环（登录/守卫/退出/401）。
  ✅ 商户域端到端（样板复制验证成立）。

  ================================================================
  ★★ 路线图（2026-07-21 结合 epay 功能重排，按优先级）★★
  ================================================================
  判断依据：epay 的核心价值是【收单主链】——
     商户调 API → 渠道下单 → 收银台/二维码 → 第三方回调 notify →
     订单更新 → 异步通知商户 → 入账/结算。
  我们四端界面已全有，但这条链一条没通，后端目前只有 登录+订单查询+商户查询
  三个只读接口。故以下以"打通收单主链"为主轴；后台其余页面切真接口是配套，非主线。

  ----------------------------------------------------------------
  阶段 A — 收单核心 MVP（最高优先，先啃风险）✅ 全部完成（2026-07-21）
  ----------------------------------------------------------------
  目标：一笔钱能真正从下单走到支付成功回调。先用【自建 mock 渠道】跑通全链路，
        不依赖真实第三方资质。做完这步，0538pay 才第一次是"支付系统"而非"支付管理界面"。
    ✅ A1 下单接口 POST /api/pay/submit（易支付协议兼容：pid+参数+MD5 签名验签、
          幂等、金额 decimal）—— 2026-07-21 完成并真跑验证。
          · pkg/sign MD5 验签逐字节对齐 epay（基准由 epay php8.2 现场算，Go 单测 3 绿）
          · uid+out_trade_no 幂等 + 参数变化拦截（同单三提只落一条，DB 核实）
          · 全链路 curl：正确签名下单→二维码链接；错签/改额/非法金额/商户不存在均正确拦截
    🔨 A2 渠道插件契约落地：PaymentChannel interface（下单/查单/回调验签）+ registry
          —— mock 渠道已实现并自注册跑通（Create 返回二维码收银台链接）；
             Refund/Close 及真实渠道待 B 阶段。
    ✅ A3 收银台/二维码中间页（2026-07-21 完成）：GET /api/pay/order/{trade_no} 公开查单
          + 前端 CashierMock.vue(/pay/mock/cashier/:trade_no)：展示订单+二维码占位+「模拟支付成功」
          按钮触发回调 → 跳 /payok；已支付订单自动跳成功页防重复。前后端 build 全绿 + dev 真跑 200。
    ✅ A4 回调 POST /api/pay/notify/{trade_no}：渠道解析 → 金额二次校验 → 条件UPDATE幂等改单
          → 触发入账+通知。2026-07-21 完成并真跑（含重复回调幂等验证）。
    ✅ A5 商户异步通知 do_notify：拼带 MD5 签名的回调 URL → GET 商户 notify_url → 判含 success；
          失败置 notify=1 + notifytime(首档1分钟)。真跑：mock 商户端收到并回 success。
          （重试队列本身留待阶段 E cron）
    ✅ A6 订单入账 + 资金流水：AccountRepo.ChangeUserMoney（事务+行锁 FOR UPDATE + pay_record 插入），
          对齐 epay changeUserMoney。真跑：余额 1580.60→1668.60(+88)，pay_record 落 1 条(action=1 订单收入)。
    ✅ 验收达成（2026-07-21）：完整用户路径真跑——下单→拿 pay_url→收银台查单→模拟支付
       →订单 status=1→余额入账(+66)→流水落库→商户 notify 收到 success→收银台复查 status=1。
    ★★ 阶段 A（收单核心 MVP）全部完成 ★★ —— 收单主链首次端到端跑通，0538pay 从"支付管理界面"
       正式成为"支付系统"。下一步进阶段 B（真实渠道接入）。

  ----------------------------------------------------------------
  阶段 B — 真实渠道接入（P0 主流 3~5 家）
  ----------------------------------------------------------------
    🔨 B1 支付宝当面付扫码（2026-07-21，结构+单测+回调端到端真跑，下单待真实凭证）
       —— pkg/alipay：RSA2 签名/验签，getSignContent(ksort/跳空/&k=v 拼接)逐字节对齐 epay 内置
          cccyun/alipay-sdk 的 AopClient。单测自生成密钥：待签串格式、签名 round-trip、篡改验签失败 全绿。
       —— internal/channel/alipayf2f：Create→alipay.trade.precreate 拿 qr_code；
          Notify→表单 RSA2 验签(支付宝公钥)+trade_status=TRADE_SUCCESS 判定+金额；Query 占位。
          复用 B2 建的 config 注入链路(seller_id 进 Extra)；main 匿名导入自注册。
       —— 前端：pluginConfigFields 加支付宝当面付预设(appid/private_key/public_key/seller_id/notify_url)；
          pluginsByType type=1 首项换 alipayf2f。
       —— **真跑端到端(真实 RSA2 签名表单回调，非 mock)**：建通道+存密钥+下单落库→
          POST 真签名回调→验签通过→订单 status=1、api_trade_no 落库、分成入账、pay_record 流水、
          幂等(重复回调余额不变) 全部通过。下单连支付宝网关需真实私钥(用户暂不提供)。
    🔨 B2 微信 Native V3（2026-07-21，结构+单测优先，未接真实凭证）
       —— pkg/wxpayv3：手写 APIv3 密码学原语(不引第三方 SDK，与 pkg/sign 一致)——
          RSA-SHA256 Authorization 头签名 + 应答/回调验签(平台公钥) + AES-256-GCM 回调解密。
          单测自生成 RSA/AES 密钥，签名 round-trip / 验签篡改失败 / 解密错密钥失败 全绿。
       —— internal/channel/wxnative：实现 PaymentChannel。Create→POST /v3/pay/transactions/native
          拿 code_url(前端渲二维码)；Notify→验签+解密判 trade_state=SUCCESS；Query 占位(以回调为准)。
          单测覆盖 下单体组装/金额分转换/回调成功/未支付/验签失败/错误APIv3密钥 全绿。
       —— 接通密钥注入：channel.Config 扩微信字段 + service.buildChannelConfig 把通道 config JSON
          注入 Config + dispatch/Notify 载入通道配置 + handler 注入回调原始 body/Wechatpay-* 头
          (兼容表单型与 JSON 型两类回调)。main 匿名导入 wxnative 自注册。
       —— 前端：Channels「配置密钥」抽屉按插件预设字段渲染(pluginConfigFields)，wxnative 走专用
          表单(含 PEM textarea)，其它插件退回通用 key-value。
       —— 真跑：建 wxnative 通道 + 存微信密钥 config + 回填 通过；mock 收单链回归(下单→回调→success)通过。
          **待真实/沙箱商户凭证到位后做端到端真跑**。
    ✅ B3 免签渠道 epay（二次对接易支付 V1/MD5，2026-07-21）——首次真实渠道下单端到端真跑
       —— internal/channel/epay:Create(MD5签名POST上游submit)+Notify(验签+trade_status)+Query占位,
          复用pkg/sign,单测5绿。config:appid/appkey/appurl/uptype/notify_url基址。
       —— **真跑闭环(无资质无外部依赖)**:epay通道指向本站自己→商户下单type=epay→真实HTTP下单上游
          (uptype=mock终结)→拿收银台二维码→上游mock支付→回调本站→epay渠道验签→入账status=1。
          两层订单均翻转,伪造签名被拦。补上"真实渠道下单从未真跑"缺口(微信/支付宝下单仍待真实凭证)。
       —— 缺:金额微调防并发、vmq个人码、epayn(V2/RSA)。
    ⬜ B4 每家按 A2 interface 实现 + 后台通道配置页（config JSON 密钥表单）切真接口
       —— B4 的「后台通道配置页按插件定制密钥表单」已随 B2 起步(pluginConfigFields 机制已建)。
    ✅ B 收银台真实二维码（2026-07-21，横切）：下单 dispatch 回填 pay_type/qr_code 到订单，
       CashierView 输出，收银台前端 qrcode 库渲染真实渠道 code_url/qr_code + 3 秒轮询查单自动跳成功页；
       mock 渠道仍走模拟支付按钮。解锁 wxnative/alipayf2f 等扫码渠道的收银台可视化。缺 H5/App 唤起。
    ✅ B 主动查单对账 Query（2026-07-21，横切）：wxnative.Query(GET out-trade-no/{no}+验签)、
       alipayf2f.Query(alipay.trade.query)，parseQueryResp 纯函数判 trade_state 均单测全绿。
       后端 GET /api/pay/query/:trade_no：本地未付时主动问渠道，渠道确认已付则走回调同一路径改单+入账(幂等)；
       mock 渠道不查(以模拟回调为准)。收银台前端轮询改调它。对齐微信"不能仅依赖回调需结合查询"。
       真跑：mock 未付→status0、回调后→status1、渠道查单失败优雅降级不崩、不存在订单报错，全通过。
       为阶段 E 定时对账打地基（cron 可复用 QueryStatus 扫未付单）。

  ----------------------------------------------------------------
  阶段 C — 后台业务域横向切真接口（配套收单，按已固化五层样板）
  ----------------------------------------------------------------
    ✅ C1 通道管理 channels（B 阶段依赖它配密钥，优先）—— 列表+写操作全部切真接口
       —— 2026-07-21 列表切真接口完成：pay_channel 表(含 config 密钥字段) + GET /api/admin/channels
          (筛选/分页) + Channels.vue 接真数据 + seedchannels(mock/支付宝/微信/QQ 4条)。
          下单已接费率：按 type 选已开启通道读 rate 算 getmoney(商户分成)，回调按分成入账。
          真跑验证：money=100→通道rate=2%→getmoney=2.00→余额+2.00入账、流水订单收入2.00。
       —— 2026-07-21 写操作切真接口完成：POST/PUT/DELETE /api/admin/channels 系列
          (create/update/delete/setStatus + config get/save) + repo 写方法 + service 校验。
          前端 Channels.vue：新增/编辑抽屉(type↔plugin 联动)+复制+删除确认(Modal)+
          状态开关调真接口(失败回滚)+密钥配置抽屉(通用 key-value JSON 编辑)。
          真跑验证：增/改/删/状态/密钥 + 各校验分支(空rate 400、rate≥100、mode=1强制daytop=0、
          非法JSON拦截、删后再改报通道不存在) 全部通过。
          剩：密钥表单按插件定制留 B4；行操作「订单/测试支付」仍占位。
    🔨 C2 结算 settle + 批次 batch（2026-07-21 后端切真接口，资金流水 record 待单独切）
       —— pay_settle + pay_batch 两表 + SettleRepo(明细/批次CRUD+收批+余额扣减退回，事务) +
          SettleService(calcFee 计费对齐epay+批次生成+状态流转+RunAutoSettle) +
          5 个接口挂 admin 分组(列表/批次列表/生成批次/批次完成/单条状态变更) +
          Settle.vue 全切真接口(批次卡片/概况/明细表/生成批次/批次完成/行操作/删除退回确认Modal/批量)。
       —— 计费单测 calcFee 全绿(常规/封底/封顶/门槛/极小额)；真跑端到端：seed 27明细3批次→
          生成批次收批3条(0→2)→二次生成正确拦截→批次完成(2→1)→删除退回(uid余额精确+200)全通过。
       —— ✅ C2 尾巴 资金流水 record 页(Records.vue)已切真接口(2026-07-21)：GET /api/admin/records
          (uid/类型/column+value/时间范围+分页) + /records/stats(统计)，复用 RecordRepo(recordFilters 共用)。
          真跑各筛选维度+统计口径全绿。缺：真实转账打款归C3；计费配置迁移见优化待办#5。
    🔨 C3 代付 transfer / 分账 profit-sharing
       —— ✅ 代付 transfer 切真接口(2026-07-21)：pay_transfer 表(主键 biz_no 幂等键) + TransferRepo
          (发起即时扣款 CreateWithDebit / 退回退款 FailWithRefund，事务+行锁+条件UPDATE防重复退款) +
          TransferService(后台免费发起 uid=0 / 商户计费扣款 + 完整校验) + 后台6接口 + 商户端2接口 +
          Transfer.vue/TransferRecords.vue/MerchantTransfer.vue 全切真接口。
          真跑资金安全全绿：扣款 389.8→289.3(-100.5)、退回 289.3→389.8(+100.5)、重复退回防双退、
          流水"代付/代付退回"对称、19位交易号/限额/余额不足/两位小数/scope隔离 全部正确。
          含补 C2 尾巴"真实转账打款"：DB 层资金流转已完成，真实渠道打款待凭证(同收单下单)。
       —— ✅ 分账 profit-sharing 切真接口(2026-07-21)：pay_ps_receiver(规则/接收方,3级优先匹配) +
          pay_ps_order(分账明细) + pay_order.profits(命中规则id) + ProfitRepo(匹配/建单/成功扣款/取消回退退回,
          事务+行锁+条件UPDATE防重复) + ProfitService(下单匹配规则→支付成功按比例建分账单) 挂进收单主链 +
          3接口 + ProfitSharing.vue 全切真接口。真跑全链路：下单命中规则→支付→自动建分账单10.00→提交扣款
          (391.8→381.8)→回退退回(→391.8)→重复回退拒绝→流水对称→editmoney→未扣款cancel余额不变。
          真实渠道分账API待凭证。接收方规则管理UI前端无(epay商户端也未实装,如实不造)。
    ✅✅ C3 阶段（代付 transfer + 分账 profit-sharing）全部切真接口并真跑通过。
    ✅ C4 风控 risk / 黑名单 blacklist / 域名 domain 切真接口(2026-07-21)：
       —— 风控 pay_risk(只读,系统写入)：关键词屏蔽 type=0 接进收单链(命中记录+拦截)；成功率/通知失败/投诉率待定时任务。
       —— 黑名单 pay_blacklist：CRUD+统计+IsBlocked 接收单链(IP命中拦截"系统异常无法完成付款",维度仅IP+账号)。
       —— 授权域名 pay_domain：CRUD+审核(0待审/1正常/2拒绝)+批量+下单白名单校验(域名或*.主域名通配,status=1才放行)。
       —— 三页 Risk/Blacklist/Domains.vue 全切真接口。真跑全绿：关键词拦截落风控记录、IP黑名单拦截、
          域名白名单强制(未授权拦/已授权放行)。下单拦截经 PayService.SetRiskServices 注入收单主链。
    ✅ C5 统计 statistics / 登录日志 log / 邀请码 invitecode 切真接口(2026-07-21)：
       —— 统计：无独立表，StatRepo 实时聚合 pay_order/pay_transfer；StatService 交叉透视(method type/channel ×
          口径0订单/1支付/2分成/3利润/4代付) + GET /admin/stat/pay。MerchantStats.vue 切真接口，真跑三口径全对。
       —— 登录日志：pay_log(epay pre_log 本质是登录日志) + 登录写入 hook(后台/商户登录成功失败) + GET /admin/logs。
          Logs.vue 切真接口。真跑：失败/后台/普通登录三类记录 + uid 精确筛选。
       —— 邀请码：pay_invitecode + 5接口(列表/生成8位随机/删除/清空全部/清空已用) + Verify/MarkUsed 注册核销预留。
          InviteCodes.vue 切真接口，真跑生成/删除/清空。regcode 是 OTP 表非注册码，epay 无管理页故不做。
          BuyerStats/OpLogs epay 无对应实体(buyerstat 菜单有文件缺、pre_log 只记登录)，保留 mock 如实标注。
    ★★★ C 阶段（C1 通道 / C2 结算+流水 / C3 代付+分账 / C4 风控+黑名单+域名 / C5 统计+日志+邀请码）全部切真接口完成！
       后台业务域横向铺开收官，绝大多数后台页面已从 mock 切到真接口。下一步可选 D3 增值项 / V2 协议 / 官网CMS接后端。

    ✅ C6 商户管理 + 用户组管理 写操作（2026-07-21）：后台业务写操作切真接口第一批。
       —— 商户写操作：添加(手机/邮箱唯一+至少一项,随机32位密钥,可选bcrypt密码) + 编辑(money直接覆盖不走流水,对齐
          epay editUser 管理员强改) + 余额充值/扣除(走 changeUserMoney 事务+行锁+流水,扣款超余额自动截断) +
          改用户组+有效期(空=永久) + 权限状态切换(封禁/支付/结算,field 分派对齐 setUser) + 重置密钥 + 删除。
          7 接口挂 admin 分组 + MerchantService 扩写方法(MerchantError 业务码) + MerchantRepo(Create/Delete/查重/组降级/组内计数)。
       —— 用户组 CRUD：pay_group 扩 config/settings/visible 字段。列表(派生费率+该组商户数) + 新增/编辑(组名唯一,
          info/config 校验合法JSON) + 上下架 + 删除(gid=0不可删,级联该组商户回落默认组)。5 接口 + GroupService(GroupError)。
       —— Merchants.vue/Groups.vue 全切真接口(抽屉表单+充值/改组/重置/删除确认弹窗)；商户列表 groupname 改 JOIN 派生 +
          到期用户组惰性降级。真跑全绿：商户 增/改/充扣(截断+流水)/改组/权限/密钥/删,各校验分支(dup邮箱/都空/坏JSON/删gid=0)，
          用户组 CRUD/上下架/删组级联降级(商户gid→0) 全部通过。
       —— 缺:group.php 购买价批量保存页(售价/有效期/排序已并入编辑抽屉);删商户级联 subchannel(待通道体系增强)。

    ✅ C7 订单管理写操作（2026-07-22）：后台业务写操作第二批。
       —— 退款(手动/API原路)：对齐 epay Order::refund，status∈[1,2,3] 可退、money≤realmoney、部分退款累加 refundmoney，
          扣余额 reducemoney 四分支 1:1(冻结单/直清=0 / 手续费不退全额=realmoney / 手续费退且全额或≥分成=getmoney / 部分=退款额)，
          changeUserMoney type=订单退款 幂等；手动退款不调渠道，API退款需管理员密码+渠道原路退款待凭证。
       —— 冻结/解冻(1↔3 扣/加 getmoney 走流水)、改未完成/改已完成(裸改状态不入账)、手动补单(status0→1 复用 settle 入账+分账+通知)、
          重新通知(复用 ResendNotify)、删除(物理删)、批量(改状态/冻/解/删逐条)。
       —— OrderService 扩写方法(SetWriteDeps 注入 account/channel/admin/pay，OrderError 业务码) + OrderRepo(SetStatus/
          SetStatusFrom/Delete/MarkRefundedPartial/FillOrder) + PayService.SettleOnFill。10 接口挂 admin 分组。
       —— Orders.vue 全切真接口：行操作按状态动态菜单接真调用 + 退款弹窗(查可退金额)+ 操作确认弹窗 + 批量勾选/操作条。
          真跑全绿：冻结/解冻净零对称、手动退款 status→2 扣分成、API退款错密码拒、补单入账落流水、批量 affected=2、删除+边界拒。
       —— 缺:预授权资金支付/解冻+直付通结算(status=4/settle，依赖支付宝真实渠道凭证)、独立订单详情页/退款统计报表。

    ✅ C8 系统设置 config 域（2026-07-22）：建 config KV 域 + 收口硬编码常量（解优化待办#5）。
       —— pay_config KV 表(对齐 epay pre_config，键名对齐 set.php 表单 name) + ConfigRepo(All/SetMany upsert) +
          ConfigService(内存缓存全量 + 类型化 getter Dec/Int/Bool/Str + configDefaults 默认值 + configGroups 分组白名单 +
          GetGroup/SaveGroup)。启动 Load 进缓存；保存后 Load 重建 + OnChange→service.ApplyConfig 实时刷新业务常量(无需重启)。
       —— 收口原散在 5 个 service 的硬编码常量为可配置：settle.go(settle_rate/money/fee_min/fee_max/type/maxlimit)、
          transfer.go(transfer_rate 空则复用 settle_rate/minmoney/maxmoney/maxlimit)、order_write.go(refund_fee_type)、
          merchant_center.go(user_deposit_min/cert_money)、pay.go(blockname 屏蔽词/blockalert)。常量改 var + reloadXxx 函数。
       —— GET/PUT /api/admin/config/:group + config api 模块 + SettleSettings.vue/PaySettings.vue 切真接口。
          真跑：get/save/白名单过滤(外来键忽略)/未知分组拒/持久化，改 settle_rate=1.2 即时流入商户 apply-info(reload 链验证)。
       —— 管理员密码/支付密码 epay 明文存 config，我们不抄(admin 密码走 bcrypt sys_admin)。

    ✅ C8b 剩余系统设置页切真接口 + 导航归属优化（2026-07-22）：
       —— config 域扩展分组 reg/oauth/notice/cert/mail/cron/other(iptype+proxy)，configDefaults 补枚举开关默认("0")避免前端下拉空选。
       —— 前端切真接口：RegSettings(reg,布尔开关字符串适配)/OAuthSettings(oauth)/NoticeSettings(notice)/CertSettings(cert)/
          MailSettings(mail,mail_*+sms_*合并)/CronSettings(cron)/OtherSettings(other)。各页 onMounted 拉取 + 保存推后端。
       —— 导航归属优化(nav.ts)：登录日志→权限管理组(与操作日志并列),网站公告→官网管理组;系统设置内按站点基础/注册登录鉴权/
          消息通知/运维语义排序。真跑:reg get/save/持久化(reg_open 1→2/reg_pay→1)、oauth/cert/other/mail 各组读写、枚举默认返回"0"。
       —— 第三方凭证类(oauth/notice/cert/mail/proxy)为纯 KV 存取(无可真跑业务闭环,被真实功能消费待各自渠道凭证)。
          缺:transfer 独立设置页(后端键已就绪被代付消费)、template/logo/account 页、注册流程消费 reg_* 待注册接口。

    ✅ C9 通道体系增强 子通道/轮询组/用户组通道分配（2026-07-22）：收单主链选通道从「按 plugin 名定位单一通道」升级为 1:1 对齐 epay 的分层分发。
       —— 三张新表：pay_roll(轮询组,kind 0顺序/1权重随机/2首个启用,info通道串,idx顺序游标) + pay_subchannel(子通道,商户维度,usetime顺序调度;
          epay apply_id 进件渠道我方无进件系统故不移植) + pay_group.info 改为 epay 格式 {typeid:{type,channel,rate}} 分配图。
       —— ChannelSelector(selectchannel.go/selecthelper.go) 1:1 移植 epay includes/lib/Channel.php 的 getSubmitInfo + getChannelFromRoll：
          按商户 gid 查组 info(缺失回退默认组 gid=0)→按支付方式 type 取分配→四分支：0关闭/-1随机(array_rand等概率)/-2子通道(usetime升序顺序调度+回写)/
          正整数(type=roll 是轮询组ID经 getChannelFromRoll 解析,否则固定通道ID)。金额 paymin/paymax 过滤、组级费率覆盖(info.rate非空优先)。
          接进 PayService.Submit(resolveChannel/resolveTypeID，旧 plugin 名路径向后兼容阶段A/B mock/epay)。三种轮询算法 + 解析函数单测 8 绿。
       —— CRUD：RollService/SubChannelService/GroupService.SaveAssigns + handlers + 路由(rolls/subchannels/groups/:gid/assigns)；
          删商户/删主通道级联删子通道(补 C6 缺口)。前端 Rolls.vue 全 CRUD、Groups.vue 通道分配编辑器、Merchants.vue 子通道管理抽屉全切真接口。
       —— 真跑全绿(选通道四分支/轮询三算法/子通道调度)：顺序轮询 1→10→1→10 精确轮转+idx持久化、固定通道 channel=10+rate1.5覆盖、
          -1随机命中、-2子通道 subA→subB→subA usetime调度、0关闭正确拒绝、组级费率 2.00% 经 roll 路径生效。
          缺：子通道 [占位]参数替换注入渠道 Config(SubInfo 已随选通道返回,待真实渠道消费)、daystatus 单日超限暂停(我方无该字段)。

    ✅ C10 V2 mapi REST 接口族（2026-07-22）：对齐 epay api.php?s= → ApiHelper 反射分发的 V2 REST 接口，成功 code=0 + 回包 RSA 签名 + timestamp 防重放。
       —— MapiService 统一验签(pid查商户/状态守卫/keytype=1强制RSA/MD5或RSA验签/timestamp±300s防重放) + 回包平台私钥RSA签名(ConfigService.EnsurePlatformKeys 启动生成密钥对)。
       —— 11 个接口全部实现并真跑：pay/create(JSON下单,pay_type+pay_info)、pay/query、pay/refund(pay_refundorder 表+out_refund_no幂等)、
          pay/refundquery、merchant/info、merchant/orders、transfer/submit(签名鉴权免密 CreateByMerchantSigned)/query/proof/balance。
          复用既有 Submit/OrderRepo/TransferService 核心，仅在此层做 JSON 契约包装+签名，不重复实现业务。
       —— 路由 /api/mapi/:class/:action(反射式分发)，老 /api/pay/submit 表单入口保留。真跑闭环：create下单→mock回调付款→query(status=1)→
          refund(reducemoney按比例)→refundquery→merchant/info统计→orders列表→transfer/submit(cost含手续费)/balance；错签-3、未知接口-5、退款幂等(重退返回同refund_no)全通过。
       —— 缺：transfer proof 真实回单 + 真实渠道打款待凭证(对齐 C3)；epay V1 api.php?act= GET 明文接口不造(已被 V2 取代)。

    ✅ C11 支付接口配置页(支付方式/公众号小程序/企业微信)切真接口（2026-07-22）：后台「支付接口」剩余配置页从 mock 切真。
       —— pay_type(支付方式,name/showname/device/status,无icon/sort对齐epay) CRUD+状态,删除护(系统自带id<4 + 被通道type引用拒删),name字母数字+name+device唯一。
       —— pay_weixin(公众号/小程序,type/name/appid/appsecret明文,无状态开关) CRUD,name+appid唯一,appsecret脱敏+编辑留空不改。
       —— pay_wework(企业微信,+子表 pay_wxkfaccount 客服账号 wid关联) CRUD+状态,删除级联删客服账号(事务),name+appid唯一。
       —— PayTypes/Wechat/Wework.vue 全切真接口 + api 模块 paycfg.ts + seedpaytypes。测试/刷客服等依赖真实微信/企微凭证的按钮如实提示待凭证。
       —— 真跑:三表 CRUD/状态/删除护/唯一校验/删除级联 全通过。踩坑:GORM 默认 WID→w_id 列名,子表关联查询需显式 column:wid。
       —— 不做(如实标注):Plugins(epay 扫目录只读,我方=channel registry,概念差异)、GetToken(依赖真实微信/支付宝授权回调无法闭环)、WxkfSettings(纯config,可后续并入config域)。

    ✅ C12 商户自助流程(注册/完善资料/找回密码)切真接口（2026-07-22）：商户中心自助流程从 mock 切真，A类缺口第一批。
       —— 图形验证码(自研 CaptchaService,SVG+token 内存存储带5min过期一次性,代替 epay 短信/邮箱 OTP+极验) GET /merchant/captcha。
       —— 注册 POST /merchant/register:reg_open(0关/1开/2仅邀请)分支 + 邀请码校验核销(复用C5 invite) + user_review→pay字段(审核态落pay,status恒1对齐epay) +
          bcrypt密码(不移植epay md5两步哈希,与现有登录一致) + 32位密钥 + 邮箱/手机格式与查重 + 图形验证码。
       —— 完善资料 POST /merchant/complete(需登录):结算方式分类型校验+姓名/QQ/域名+已完善拒重复。找回密码 POST /merchant/findpwd:账号存在+图形验证码+bcrypt重置。
       —— MerchantRegService + CaptchaService + 3 路由 + MerchantReg/Complete/Findpwd.vue 全切真接口(图形验证码替换原OTP倒计时UI)。
       —— 真跑全绿:reg_open三模式(开放注册可登录/邀请无码拒带码成功核销)、错验证码/重复邮箱/纯数字密码全拒、user_review=1待审核、完善资料空成功重复拒、找回密码新登旧拒。
       —— 缺(待外部凭证):短信/邮箱 OTP 发送、极验滑块、微信 OpenId 扫码获取、reg_pay 付费注册(依赖支付通道)。

    ✅ C13 商户中心其余自助流程(测试支付/聚合收款码/授权域名/邀请返现/使用说明/站内信)切真接口（2026-07-22）：A类缺口第二批，商户中心 mock 页收官。
       —— 测试支付 test.php:config test 分组(test_open/test_pay_uid) + MerchantSelfService.TestPay 复用 CreateInternalOrder(tid=3)走收单链 +
          金额上下限 + 可选支付方式按 registry 过滤。新建 MerchantTest.vue + 导航。
       —— 授权域名 domain.php:DomainService 加 MerchantList/MerchantAdd(status=0待审)/MerchantDelete(uid scope) + 3接口。MerchantDomains.vue 切真。
       —— 邀请返现 invite.php:config invite 分组(invite_open/invite_rate/invite_order_type/invite_order_fee) + pay_merchant.upid(注册 ?ref= 推广码解出上级) +
          InviteRewardService.SettleOnPaid 挂支付成功钩子实时返现到上级余额+流水(三口径 calcReward,封顶不超手续费) + 统计(邀请人数/今昨累计返现)。MerchantInvite.vue 切真。
       —— 聚合收款码 onecode.php:pay_merchant 加 codename/open_code + OnecodeInfo(固定收款URL+名称)/SaveCodeName + 公开收款页 Paypage.vue(/paypage,
          GET /api/paypage/info + POST /api/paypage/submit→CreateInternalOrder 走收单链→跳收银台)。MerchantOnecode.vue 切真+qrcode 渲染。
       —— 使用说明 help.php:后台可编辑(超出 epay 硬编码静态页)。config help 分组(help_content) + HelpSettings.vue(编辑预览) + MerchantHelp.vue 读后端(空则内置默认)。
       —— 站内信(★epay 无此实体,我方新增):pay_message + pay_message_read 两表。后台 Messages.vue(全体广播/定向下发) + 商户 MerchantMessages.vue(收件箱+已读+未读数)。
          epay 的 MsgNotice 是对外推送(微信/短信/邮件)非站内收件箱,故此为我方新增功能不计精仿缺口。
       —— 后端:invite_reward.go/merchant_selfservice.go/message.go 三 service + message_repo.go + message.go model(2表) + merchant_center handler 追加 + selfservice.go handler(paypage/admin messages) +
          路由(merchant 6组自助 + 公开 /paypage + admin /messages)。前端:merchantCenter.ts 追加 + paypage.ts + messages.ts + 5商户页切真+2新页 + Paypage公开页 + 后台2页 + 导航+路由。
       —— 真跑全绿:测试支付 mock 下单→收银台、域名 status=0 待审+非法拒+uid隔离、邀请返现 upid付100→上级+10+流水+统计、
          收款码 URL+名称+paypage info/submit、站内信广播+定向+收件箱 unread=2→已读 unread=1、使用说明后台存→商户读。go build✓ npm run build✓ dev 真跑各路由 200✓。
       —— 缺(待外部凭证):测试支付/聚合收款真实渠道下单(mock 可闭环,同收单主链);checkDomain 更严格通配符校验(后续)。

    ✅✅ C14 A类缺口批量清零（2026-07-22）：后台mock页 + 自研cron + 官网静态页 全部切真接口，A类(能自研闭环)与 epay 对齐收官。见 [[a-class-backend-batch-done]]。分4批:
       批1: A-2 分账自动执行cron(profitTicker) + A-6 后台仪表盘聚合(DashboardService 概况/指标/待办/趋势/实时订单) + A-9 网站公告(pay_announce CRUD) + A-10 文章管理(接 CMS KV 持久化)。
       批2: A-5 转账付款配置页(config group=transfer) + A-7 订单后端全量导出(流式CSV 17列BOM,破ExportAll 10万上限) + A-8 数据清理(白名单表删N天前,7天保护+二次确认)。
       批3: A-11 SSO免密进商户端(管理员代签scope=merchant短时JWT 10min) + A-12 结算删除退回支付密码二次校验(admin_paypwd 等价 bcrypt)。
       批4: A-4 风控设置(config group=risk) + A-3 风控自动关停cron(riskTicker:成功率/通知失败→关停pay+写pay_risk,真跑关停3商户) + A-1 cronkey保护(/api/cron/:task?key= 手动触发,空则关闭/错误403)。
       + A-13 官网静态页(协议/关于接网站设置CMS联动,支付成功失败页功能完整)。
       —— 每批 go build✓ npm run build✓ dev路由200✓,分 api/web/docs 三 commit 入库。
       —— 不做(归B/C):投诉complain(epay无实体+渠道投诉API卡凭证)、通道成功率关停(可后续)、扫码中间页视觉皮肤(数据链路已覆盖)、BuyerStats(epay无页面)、Billing(我方细化)。

  ----------------------------------------------------------------
  阶段 D — 商户中心 /m 接后端
  ----------------------------------------------------------------
    ✅ D2 商户端查询页 + 提现/退款（2026-07-21）：工作台聚合统计 + 订单查询 + 资金流水(新建RecordRepo)
       + 结算记录 + 申请提现(复用结算域CreateWithDebit) + 订单退款(余额层逆向,状态转2+退款流水+幂等)
       + 重新通知(复用通知重发)。MerchantCenterService/Handler + /api/merchant 8 接口 +
       merchantCenter api 模块 + MerchantHome/Orders/Records/Settle/Apply 5 页全切真接口。
       真跑全绿:5页渲染、余额精确联动(退款-0.14/提现-50)、超额/低门槛/重复退款正确拦截。
       缺:部分退款/支付密码校验、渠道侧真实退款(待凭证)。
    🔨 D3 资料/密钥/密码核心块（2026-07-21）：API信息(查看MD5密钥)+重置密钥 + 修改资料(结算账号/联系方式/
       扣费模式,Merchant已有字段) + 修改登录密码(bcrypt,校验旧密码)。MerchantApi/MerchantProfile/改密码弹窗
       全切真接口。真跑全绿:密钥重置、改QQ生效、改密后旧失效新可登录/错误旧密码拒绝。
       余下 D3:生成RSA/keytype(属V2待V2实现)、实名认证、充值、保证金、购买会员、消息通知/短信改绑(依赖各域)。
    🔨 D1 商户登录态（2026-07-21 完成登录闭环）
       —— 后端 MerchantAuthService(密码 bcrypt / 密钥 AppKey 明文比对 双模式 + type 纠偏 + 状态守卫,
          签 scope=merchant 的 JWT;认证语义对齐 epay 但载体用 JWT 不移植 RC4 cookie) +
          MerchantAuthHandler + /api/merchant 分组(login 免鉴权, /info 需 merchant 鉴权) +
          seedmerchants 补 bcrypt 密码(测试密码 merchant888)。
       —— 前端 merchantAuth store + lib/api/merchantAuth + client 独立 merchant_token key(与 admin 隔离) +
          MerchantLogin 切真接口 + 路由守卫 /m 分组保护(requiresMerchant) + 退出清态 + 401 分端跳登录 +
          顶栏商户信息去硬编码改从 store 读。
       —— 真跑全绿:密码登录/密钥登录/未登录拦截+redirect回跳/退出清态/scope隔离(merchant token 访问 admin 403)。
          API 密钥重置/RSA 生成归 D3。
    ✅ D3 增值项 保证金/购买会员/余额充值/实名认证 切真接口(2026-07-21)：
       —— 保证金 deposit：AccountRepo.DepositFromBalance/DepositWithdraw(余额↔保证金事务划转+流水)。余额路径真跑,渠道待凭证。
       —— 购买会员 groupbuy：pay_group 表 + BuyGroupWithBalance(扣余额+改gid+到期时间)。续期从原到期累加/新购从当前累加/永久组。
          余额支付真跑(购白银-30/续期-60到期累加),渠道待凭证。seed cmd/seedgroups。
       —— 余额充值 recharge：pay_order 加 tid 字段 + PayService.CreateInternalOrder 下内部订单 + settle() 按 tid=2 分派入账。
          mock 渠道端到端真跑(下单→回调→余额+100),真实渠道待凭证。
       —— 实名认证 certificate：pay_merchant 加 cert 明细字段 + 提交校验(身份证/姓名/工本费预检)+暂存审核中+脱敏。
          **第三方核验(支付宝/微信/阿里云)依赖外部凭证无法真跑**,提交如实返回待凭证,cert=0→1 待回调接入。
       —— 全部走 changeUserMoney 等价的锁行+流水事务(对齐 epay)。真跑各资金路径精确联动,边界拦截正确。
    ★★ D 阶段（D1 登录态 / D2 查询+提现退款 / D3 资料密钥密码+增值项）核心全部切真接口。
       剩纯外部凭证依赖项（充值/实名/购组的真实第三方渠道）待凭证。

  ----------------------------------------------------------------
  V2 协议（RSA 签名 + epayn 渠道 + keytype 开关）✅ 全部完成（2026-07-21）
  ----------------------------------------------------------------
    ✅ RSA 签名验签 pkg/sign/rsa.go：MakeRSA/VerifyRSA 复用 V1 Content() 待签名串 + SHA256WithRSA + base64,
       密钥单行base64/PKCS#8(对齐 epay pemToBase64),GenerateRSAKeyPair 2048位。单测4绿。
    ✅ Submit V2 验签分派 verifySubmitSign：keytype=1 强制RSA/keytype=0 兼容(按sign_type)，RSA 校验 timestamp ±300s 防重放。
       双向密钥流向对齐 epay:下单商户私钥签→平台用商户公钥验;回调平台私钥签→商户平台公钥验。
       真跑:MD5拒绝/RSA通过/无timestamp拒/过期拒/错误私钥拒/权限开通后RSA下单出收银台。
    ✅ epayn 渠道 internal/channel/epayn：Create 商户私钥RSA签名下单、Notify 上游平台公钥RSA验签。单测3绿。
       config: appid/appurl/platform_public_key/merchant_private_key。
    ✅ 商户 RSA 密钥管理：POST /apikey/rsa 生成(私钥一次性返回不落库)+ PUT /apikey/keytype 切模式。
       MerchantApi.vue 切真接口。真跑:未配公钥切RSA拒绝、生成密钥对、切仅RSA成功。

  ----------------------------------------------------------------
  官网 CMS 接后端（自研，非 epay 功能）✅ 全部完成（2026-07-21）
  ----------------------------------------------------------------
    ✅ pay_site_config KV 表(key→JSON longtext) + SiteConfigService(key白名单 content/docs/settings + JSON合法性校验) +
       公开读 GET /api/site/config/:key + 后台鉴权写 PUT /api/admin/site/config/:key。
    ✅ 三个前端 store 接后端:siteContent(首页营销板块)/siteDocs(开发文档)/site(网站设置)。
       每个加 hydrate()从后端拉取覆盖 + update()存后端;localStorage 保留为即时渲染/离线兜底。
       官网(SiteLayout/ClassicHome/ClassicDocs)加载 hydrate;后台(SiteContent/DocsContent/Settings)保存推后端。
       docs store 变更防抖800ms推后端且仅 admin token 时推(官网公开文档页只读不误写)。
    ✅ 真跑:存 content/docs/settings→公开读回一致、非法JSON拒、非白名单key拒、未鉴权401、UTF8 round-trip完整。
       至此官网三层内容(首页营销/开发文档/网站设置外壳)全部真持久化,后台写→官网读闭环。

  ----------------------------------------------------------------
  阶段 E — 定时/异步 + 收尾
  ----------------------------------------------------------------
    ✅ E1 cron：notify 重试队列 + 对账 + 超时关单 + 自动结算 全部打通（2026-07-21）
       —— internal/scheduler：ticker 驱动的轻量调度器(不引第三方 cron 库)，main 启动 + SIGINT/SIGTERM
          优雅停机(先停调度器再 Shutdown HTTP)。默认通知重试每 1min、对账每 5min、每批 20 单。
       —— 通知重试对齐 epay cron do=notify：notify 字段改为重试计数语义(0成功/>0待重试第N次/-1放弃)，
          退避表 1/2/16/36min/1h(notifyBackoff 纯函数单测)，超 5 次置 -1 放弃；RetryNotify 扫
          notify>0 且到期 且近一天完成的单重发，成功归0/失败递增退避。repo：SetNotifySuccess/
          SetNotifyRetry/FindNotifyPending。首次通知失败也改走该语义(fireMerchantNotify)。
       —— 对账 ReconcileUnpaid：扫最近窗口未支付真实渠道单，复用 QueryStatus 主动查渠道，确认已付则
          幂等改单入账。repo FindUnpaidForReconcile(排除 mock)。
       —— **真跑端到端**：mock 单回调时商户端返回 fail→订单 status=1 但 notify=1 入队→
          flip 商户端为 success→scheduler tick 命中 RetryNotify→重发成功→notify 归 0。全链路通过。
       —— 缺：自动结算(settle_task)、E2 风控自动关停支付权限。
    ✅ E1b 超时关单（2026-07-21）：scheduler 每小时 CleanExpiredOrders 删 status=0 且
       add_time<24h 前的未付单(对齐 epay cron do=order delete)。repo CleanExpiredUnpaid +
       service CleanExpiredOrders。真跑边界：25h未付→清、1h未付→留、25h已付→留，精确。
    ✅ E1c 自动结算（2026-07-21，随 C2 结算域建成）：scheduler 每小时 RunAutoSettle，
       服务层每日幂等锁(当天已生成自动结算单则跳过)，选达标商户生成 pay_settle 单并即时扣减余额。
       对齐 epay cron do=settle。详见 C2。
    ⬜ E2 SaaS 控制台 P2 剩余（免密登录/公告/白标）—— 低优先
    ⬜ E3 路由懒加载 / 代码分割（技术债 #1）

  ----------------------------------------------------------------
  ⏸ 挂起项（7/17 起）：优化收尾
  ----------------------------------------------------------------
    ⏸ 登录页改动（admin-web Login.vue / MerchantLogin.vue / MerchantReg.vue）、
       design-previews/、go-cleanup-admin.ps1 等仍在 git 未提交，进阶段 A 前先决定收尾/提交。

  样板套路见 memory: go-backend-bootstrap-done（后端五层 + 前端 api 模块→页面切换）。
