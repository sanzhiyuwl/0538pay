====================================================
0538pay admin-web 收尾开发清单（对齐彩虹易支付 epay）
====================================================
用途：管理后台主体页面已完成，本清单汇总对照 epay.test 后台后仍欠缺的功能，
      作为「准备完成开发」阶段的收尾任务。做完即视为主后台与 epay 功能对齐。
对照基准：epay.test/admin/head.php 菜单 + set.php 17 个 mod（2026-07-14 核对）。
格式：[状态] 编号. 标题 —— epay 对应文件 / 说明 / 落地方案
状态：[ ]未处理  [~]进行中  [x]已完成
优先级：P0 独立业务功能（菜单缺项最明显）> P1 设置页补全 > P2 工具页
----------------------------------------


====================================================
P0 · 独立业务功能（新建页面 + mock + 路由 + 菜单）
====================================================

注：投诉管理（complain.php / complain_info.php）经核对，此版本 epay.test 源码
    中并无实体文件（仅 head.php checkIfActive 里被引用，无可见菜单入口），故不做，
    避免凭空造功能。若日后拿到源码再补。（2026-07-14 核对修正）

[x] 2. 邀请码管理
    epay：invitecode.php（reg_open==2 邀请注册模式时启用）
    说明：生成 / 分发 / 作废邀请码。字段：邀请码 / 状态（未用/已用/停用）/
          绑定用户组 / 使用人 / 生成时间 / 有效期。支持批量生成、导出。
    落地：新建 InviteCodes.vue + mock/invitecodes.ts。挂「商户管理」组
          （与「用户组/套餐」并列）。

[x] 3. 支付用户统计
    epay：buyerstat.php
    说明：按【付款人】维度统计（区别于现有 merchant-stats 的商户维度）。
          字段：付款用户标识 / 付款次数 / 累计金额 / 首末付款时间 / 关联商户。
    落地：新建 BuyerStats.vue + mock/buyerstat.ts。挂「商户管理」组。

[x] 4. 独立导出订单页
    epay：export.php
    说明：Orders 页内已有「导出」按钮，但 epay 是独立批量导出页——
          按时间段 / 商户 / 状态 / 支付方式组合条件导出 CSV/Excel。
    落地：可做成 Orders 页的「高级导出」弹窗（Drawer），或独立 Export.vue。
          评估后二选一，倾向弹窗以复用筛选条件。


====================================================
P1 · 系统设置补全（对齐 set.php 17 个 mod）
====================================================
现状：已覆盖 site（网站/版权/LOGO）、pay、risk、settle 的独立页。
目标：补齐余下 mod。建议做成设置区多 Tab / 多卡片，复用现有 Settings 版式。

[x] 5. 快捷登录配置（oauth）
    说明：QQ / 微信 / 支付宝 快捷登录 AppID / AppSecret / 回调配置开关。

[x] 6. 消息提醒配置（notice）
    说明：微信模板消息 / 邮件 / 短信 通知渠道与模板开关。

[x] 7. 实名认证配置（certificate）
    说明：支付宝人脸 / 微信实名 认证方式、商户开关、费用规则。

[x] 8. 首页模板配置（template）
    说明：11 套官网模板（default + index1~10）切换与预览。

[x] 9. 邮箱与短信配置（mail）
    说明：SMTP 参数、短信服务商 AppKey、发信测试（对应 mailtest）。

[x] 10. 计划任务配置（cron）
    说明：自动结算 / 通知重试 / 订单清理 / 分账 / 风控关停 定时任务开关与频率。

[x] 11. 其余 mod（proxy / account / iptype）
    说明：代理配置、账户配置、IP 类型库。优先级最低，评估是否原型阶段需要。


====================================================
P2 · 工具 / 补充页（优先级最低）
====================================================

[x] 12. 企业微信配置
    epay：pay_wework.php
    说明：现有 Wechat.vue 只做了「公众号 / 小程序」，缺企业微信应用配置。
          可并入 Wechat.vue 增一个 Tab，或新建 Wework.vue。

[x] 13. H5 跳转微信客服支付
    epay：set_wxkf.php（独立配置页，不在普通设置里）
    说明：微信客服（wxkf）跳转支付配置。可并入公众号页 Tab。

[x] 14. 获取用户标识
    epay：gettoken.php
    说明：拉取 openid / 微信标识的工具页。使用频率低，末位处理。


====================================================
实施说明
====================================================
- 每完成一项：更新本文件状态位，并按 self-check 流程自检（构建 / 规范 / 业务 / 交互 / 真跑）。
- 新页面必须遵循已定稿的后台 UI 设计规范（直角无边框卡片 / Panel / .tbl 表格 / Badge）。
- 路由 / 菜单 / mock 三处同步：nav.ts 加菜单 → router 注册 → mock 出数据。
- 全部做完后统一处理 优化待办.txt（首要是路由懒加载）。
发现日期：2026-07-14

====================================================
完成记录（2026-07-14）
====================================================
全部 13 项（除已核对不做的投诉管理）开发完成并通过自检：
- 新增页面：InviteCodes / BuyerStats / OAuthSettings / NoticeSettings /
  CertSettings / TemplateSettings / MailSettings / CronSettings / OtherSettings /
  Wework / WxkfSettings / GetToken；Orders 增「高级导出」抽屉。
- 新增 mock：invitecodes.ts / buyerstat.ts / sysconfig.ts / wework.ts。
- 三处同步：nav.ts 菜单 + router 注册 + mock 数据，均已核对（12 条新路由 nav=1/router=1，无占位回退）。
- 自检结果：
  · 构建 vue-tsc 类型检查零错误；vite build 成功（chunk>500KB 为既有优化项，见 优化待办.txt #1）。
  · 真跑 dev server（:5174）：12 个视图 + 4 个 mock 模块经 Vite 转换均 200，SPA 正常服务。
  · 规范：全部沿用 Panel / .tbl / Badge / row-field / filter-bar / Drawer 既有组件与样式。
