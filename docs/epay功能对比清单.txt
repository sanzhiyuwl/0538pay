================================================================
0538pay ✕ 彩虹易支付(epay) 功能逐项对比清单
================================================================
用途：以 epay.test 全部功能为基准，逐条列出「我们做到哪一步」。这是"精仿完整度"
      的权威核对表。判断某功能有没有、做到几成，先查本文件；进度节奏看 开发进度总览.txt。
生成方式：4 个只读子代理分头通读 epay 源码(admin/user/收单链路/插件) + 清点 0538pay 现状，
      合成本表（2026-07-21）。
可信度：★ 已用真实代码校准（2026-07-21）。3 个只读代理逐项核对 admin-web 前端(mock vs
      真接口/文件是否存在) + 0538pay-api 后端(路由/表/渠道/cron)。校准结论：清单描述几乎
      全部属实；仅纠正 1 处——商户登录页「极验验证码」由 🎨 降为 ⬜(代码里根本没做，
      极验只在后台配置项)。后端"4 路由/3 表/渠道零实现/无cron/无收单API"经代码逐条确认属实。
      故本表状态可作代码级可信，非结构推断。
最后更新：2026-07-21（代码校准）
对照基准：epay.test（彩虹易支付，VERSION=3085，原生PHP，51插件，约29表）。
我们方案：admin-web(Vue3四端,前端已成型) + 0538pay-api(Go/Gin/GORM，后端刚起步)。

----------------------------------------------------------------
图例（每项标注"前端UI 状态 / 后端API 状态"两轴）
----------------------------------------------------------------
  ✅ 完成    前后端打通、真库真接口、跑通验证
  🎨 仅前端  UI 页面已建，但数据是 mock，后端无对应接口
  ⬜ 未实现  该轴该做但还没做
  ▪ 不适用  该轴天生没有此项（纯后端能力如回调/验签本就无前端页；用在前端轴上）
  ➖ epay无  我们独有、epay 没有的东西（如 SaaS 控制台）
  ⏸ 暂缓    有意推迟
  说明：双轴项标"前端UI / 后端API"。前端轴出现 ▪ = 这是纯后端能力，不该有前端页面，
        不是漏做；只有 ⬜ 才表示"该做没做"。

一句话现状：前端 4 端约 90+ 页面几乎全部建好(绝大多数 mock)；后端只有
  "登录 + 订单列表 + 商户列表" 3 个真接口。epay 的心脏——对外收单 API、
  支付渠道插件、回调、定时任务——全部为零。精仿完整度：界面≈90%，后端能力≈5%。


================================================================
第一部分 ★ 对外收单 API + 支付全链路（epay 核心价值，我们最缺）
================================================================
说明：这是易支付的心脏。epay 有 V1(旧,MD5签名) 和 V2(新,RSA签名) 两套对外协议，
      商户系统靠它们下单/查单/退款/代付。我们目前 /api/pay/* 整个不存在。

【下单 / 收银】
  ⬜/🔨  V1 页面下单  submit.php（GET 跳转到收银台/直接支付）
        —— 后端 POST /api/pay/submit 已打通(MD5验签/幂等/decimal金额/mock渠道下单)，
           2026-07-21 真跑验证：正确签名下单→返回二维码链接；错签/改额/重复单/非法金额均正确拦截；
           同 out_trade_no 三次提交只落一条订单(幂等成立)。缺：收银台页渲染(A3)、真实跳转。
  ▪/⬜  V1 API下单   mapi.php（返回 JSON，商户自渲染）
  ▪/⬜  V2 下单      pay/submit、pay/create（RSA 签名，REST 风格）
  🎨/✅  收银台聚合页 cashier.php（无 type 时展示可选支付方式）
        —— A3 完成：GET /api/pay/order/{trade_no} 公开查单 + 前端 CashierMock.vue
           (/pay/mock/cashier/:trade_no) 展示订单+二维码占位+「模拟支付成功」触发回调→跳 /payok。
           mock 渠道无真实二维码/多支付方式选择(留待 B 阶段真实渠道)。2026-07-21 真跑通过。
  ⬜/⬜  支付中间页   includes/pages/（扫码页/H5/JSAPI/公众号，按插件返回 type 渲染）
        —— 我们后端 internal/channel 有 interface 抽象，但零渲染、零下单入口。

【查单 / 退款】
  ▪/⬜  V1 单笔查询  api.php?act=query
  ▪/⬜  V1 批量查询  api.php?act=orders
  ▪/⬜  V1 退款      api.php?act=refund
  ▪/⬜  V2 查单/退款/退款查询  pay/query、pay/refund、pay/refundquery

【回调 / 通知（最关键的异步链路）】
  ▪/✅  第三方回调路由  /api/pay/notify/{trade_no} → 渠道 Notify() 解析(验签在渠道层)
  ▪/✅  订单状态幂等更新 条件 UPDATE(status IN 0,4→1)，影响行数判重复回调，对齐 Payment::processOrder
  ▪/✅  订单入账/结算出账  AccountRepo.ChangeUserMoney(事务+行锁+pay_record流水)：普通订单入账(add)已通；
        结算生单出账/删除退回(C2 SettleRepo CreateWithDebit/DeleteWithRefund，含余额校验)已通。
        充值/买组/保证金等按 tid 分支留待商户中心(阶段D)
  ▪/✅  商户异步通知     buildCallbackParams(MD5签名) + do_notify(GET notify_url 判 success)
  ▪/✅  通知失败重试     notify=重试计数，scheduler 每1min跑RetryNotify，退避1/2/16/36min/1h对齐epay，超5次放弃；真跑通过(见第三部分 cron)
  ▪/🔨  同步跳转 return  支付完成跳回商户 return_url —— return_url 已落库，收银台支付成功后
        跳 /payok(带 return_url)，"返回商户"按钮已可跳回；带签名参数的标准 return 回跳待完善
        —— 2026-07-21 全链路真跑：下单→回调→status=1→余额+88入账→pay_record落1条→
           商户notify收到success；重复回调幂等(余额不再变、流水仍1条)。

【商户信息 / 代付 API】
  ▪/⬜  V2 商户信息     merchant/info
  ▪/⬜  V2 商户订单     merchant/orders
  ▪/⬜  V2 代付提交/查询/凭证/余额  transfer/submit|query|proof|balance

【签名 / 安全】
  ✅  MD5 签名验签（V1：pid+key，ksort 去空去 sign）
      —— pkg/sign 逐字节对齐 epay getSignContent+makeSign+verifySign，
         基准由 epay php8.2 现场计算，Go 单测 3 个全绿(2026-07-21)。
  ⬜  RSA SHA256 签名验签（V2：平台私钥签、商户公钥验）—— Merchant 已留 keytype/publickey 字段
  ⬜  timestamp 防重放（V2，±300s）
  ✅  下单幂等（uid+out_trade_no）—— submit 幂等 + 参数变化拦截；回调侧条件 UPDATE 幂等改单
  ✅  回调金额/状态二次校验防篡改 —— 回调金额 decimal 精确比对订单金额，不一致拒绝
  —— MD5验签 + 下单/回调双向幂等 + 回调金额校验已落地(阶段A1+A4)；RSA/timestamp 待 V2。


================================================================
第二部分 ★ 支付渠道插件体系（epay 51 插件，我们 0 实现）
================================================================
说明：epay 用"约定式插件"——plugins/{name}/{name}_plugin.php，每个类实现
      submit/notify/return/refund/qrcode/jspay/h5/transfer 等方法，静态 $info 定义元数据
      (name/showname/types/inputs密钥表单/select子产品 等)，init 注册到全局。
      我们改为 Go interface + registry(自研不抄约定)，但目前只有 interface 定义，零渠道落地。

【插件框架】
  🔨  插件契约（Go PaymentChannel interface：Create/Query/Notify）
      —— interface 抽象已写(internal/channel/channel.go)，mock 已按其实现；Refund/Close 待需求补
  ✅  registry 注册表 —— mock 渠道 init() 自注册，server 匿名导入触发；Get/Keys 可用
  ⬜  插件元数据 / 密钥表单动态渲染（epay 据 $info.inputs 渲后台配置表单）
  ✅  mock 模拟渠道 —— internal/channel/mock，Create 返回二维码链接 + Notify 解析回调；
        2026-07-21 下单→回调→入账→通知 全链路真跑通过

【支付方式 type（epay 6 种）】
  🎨/🔨  支付宝 alipay   （当面付扫码后端已打通，见下方插件区 alipayf2f）
  🎨/🔨  微信 wxpay（Native 扫码后端已打通，见下方插件区 wxnative）
  🎨/⬜  QQ钱包 qqpay
  🎨/⬜  网银 bank
  🎨/⬜  京东 jdpay
  🎨/⬜  PayPal paypal

【渠道层级（epay：type→plugin→channel→subchannel→roll 轮询）】
  ✅  支付通道 CRUD（前端 Channels 页）
        —— C1 列表已切真接口：pay_channel 表(含 config 密钥字段) + GET /api/admin/channels
           (名称/ID/插件/方式/状态筛选) + Channels.vue 接真数据；下单已接费率(选通道读 rate 算 getmoney)。
           2026-07-21 真跑：money=100→通道rate=2%→getmoney=2.00→回调按分成入账。
        —— C1 写操作已切真接口(2026-07-21)：POST/PUT/DELETE /api/admin/channels 系列
           (create/update/delete/setStatus + config get/save)。校验对齐 epay：name/rate/plugin 必填、
           rate/costrate 0~100、mode=1 强制 daytop=0、type 派生 typename/typeshowname、config 校验合法 JSON。
           前端：新增/编辑抽屉(type↔plugin 联动)、复制、删除确认(Modal)、状态开关调真接口(失败回滚)、
           密钥配置抽屉(通用 key-value JSON 编辑)。真跑验证：增/改/删/状态/密钥 + 各校验分支全通过。
           缺：密钥表单按插件定制(留 B 阶段)、订单/测试支付两个行操作(占位)。
  🎨/⬜  子通道 subchannel（商户自定义）
  🎨/⬜  轮询组 roll（3 种模式：顺序/加权随机/固定优先）
  🎨/⬜  用户组通道分配（channel=0关/-1随机/-2子通道/正整数固定，组级覆盖费率）

【51 个真实渠道插件（epay 全集，我们按 P0/P1/P2 分级逐步做）】
  P0 主流直连（阶段 B 目标 3~5 家）：
    🔨  支付宝系（alipay 当面付/网页/预授权）
        —— alipayf2f(当面付扫码) 后端已实现并单测(2026-07-21，阶段B B1)：
           pkg/alipay(RSA2 签名 getSignContent 逐字节对齐 cccyun/alipay-sdk AopClient +
           私钥签名 + 支付宝公钥验签，单测自生成密钥全绿) + internal/channel/alipayf2f
           (Create→alipay.trade.precreate 拿 qr_code、Notify 表单 RSA2 验签+trade_status 判定、
           Query→alipay.trade.query 主动查单) + 复用 config 注入(seller_id 进 Extra) + 前端 pluginConfigFields 支付宝预设字段。
           **真跑端到端验证通过**：真实 RSA2 签名表单回调→验签通过→订单翻转 status=1、
           api_trade_no 落库、分成入账、pay_record 流水、幂等(重复回调余额不变)。
           **下单需真实凭证才能连支付宝网关**(用户暂不提供)，回调链路已用真签名跑通。
           2026-07-21 补主动查单：alipay.trade.query，parseQueryResp 判 TRADE_SUCCESS/FINISHED
           (TRADE_NOT_EXIST 视未付非错误)，单测全绿。
           缺：网页/APP/JSAPI/预授权、退款。
    🔨  微信系（wxpay Native/JSAPI/H5，wxpayn V3）
        —— wxnative(微信 Native V3 扫码) 后端已实现并单测(2026-07-21，阶段B B1)：
           pkg/wxpayv3(RSA-SHA256 Authorization 签名 + 应答/回调验签 + AES-256-GCM 回调解密，
           单测自生成密钥全绿) + internal/channel/wxnative(Create→/v3/pay/transactions/native
           拿 code_url、Notify 验签+解密判 trade_state、Query 占位) + 通道 config JSON 注入
           channel.Config + handler 注入回调原始 body/Wechatpay-* 头。前端「配置密钥」抽屉
           按 wxnative 插件预设字段渲染(appid/mch_id/serial_no/api_v3_key/private_key/
           public_key/notify_url)。**未接真实商户凭证**(用户暂不提供沙箱)，真凭证到位后走沙箱端到端。
        —— 2026-07-21 补主动查单 Query：GET /v3/pay/transactions/out-trade-no/{no}，RSA 签名+验应答签+
           trade_state=SUCCESS 判定，parseQueryResp 纯函数单测全绿。
           缺：JSAPI/H5、退款。
    ⬜  个人免签一家（vmq / xorpay / ltzf / xunhupay 之一，无资质兜底）
  P1 二次聚合 / 国际：
    ⬜  epay/epayn（二次对接其它易支付）
    ⬜  paypal / stripe / alipayg / wxpayng（国际）
  P2 持牌机构（资质门槛高，最后做）：
    ⬜  allinpay / lakala / yeepay / chinaums / swiftpass 等
  —— epay 共 51 个插件目录，我们不追求 51 个全做，按能拿到资质 + 需求分级实现。


================================================================
第三部分 ★ 定时任务 cron（epay cron.php，我们完全没有）
================================================================
说明：epay cron.php(423行) 是运维心脏。我们前端有 /admin/cron-settings 配置页(mock)。
      2026-07-21 后端起了 internal/scheduler(ticker 驱动，不引第三方 cron 库)，通知重试+对账已真跑打通。

  ✅  自动结算 settle（到期批量结算 + 余额扣减）
        —— C2 结算域已建(2026-07-21)：scheduler 每小时跑 RunAutoSettle(服务层每日幂等锁，
           当天已生成自动结算单则跳过)，选 余额≥门槛(30)+结算权限开+状态正常+填了账号姓名 的商户，
           按 calcFee(0.5%,封底0.1封顶20)算 realmoney，生成 pay_settle 单并即时扣减商户余额(出账+流水)。
           转账打款(transfer_*)归代付域(C3)，商户端申请提现归 D 阶段。
  ✅  超时订单关单 / 清理
        —— scheduler 每小时跑 CleanExpiredOrders：删 status=0 且 add_time<24h 前的未付单(对齐 epay
           cron do=order 的 delete)。真跑边界验证：25h未付→清、1h未付→留、25h已付→留。清理统计(日报表)待补。
  ✅  异步通知 notify 失败重试（多档退避）
        —— internal/scheduler ticker 每 1min 跑 RetryNotify；notify 字段=重试计数(0成功/>0第N次/-1放弃)，
           退避 1/2/16/36min/1h 对齐 epay cron do=notify，超 5 次放弃。repo FindNotifyPending
           (notify>0 且到期 且近一天完成)。真跑：回调时商户 fail→入队→flip success→cron 重发→notify 归0。
  🔨  未支付订单主动对账（我们扩展，epay 靠 notify2 手动兜底）
        —— scheduler 每 5min 跑 ReconcileUnpaid，扫最近窗口未付真实渠道单，复用 QueryStatus 查渠道→幂等改单入账。
  🎨/⬜  分账 profitsharing 自动执行
  🎨/⬜  风控 check（触发规则自动关停商户/通道）—— epay 连续N单通知失败自动关支付权限，待补
  🎨/⬜  投诉 complain 自动处理（冻结/回复/拉黑/退款）
  🎨/⬜  cron 访问密钥保护
  —— 通知重试+对账已打通；自动结算/超时关单/分账/风控 待接后端阶段 E 续做。


================================================================
第四部分 管理后台（epay admin 49 文件 → 我们 AdminLayout）
================================================================
说明：这是我们完成度最高的部分——UI 几乎全部对齐 epay 并通过自检。
      绝大多数是 🎨(前端建好/mock)，仅订单列表、商户列表已切真接口。
      标注格式：epay菜单/文件 → 我们视图 [状态]。

【平台首页】
  🎨  仪表盘统计（订单数/商户数/余额/结算/成功率/收入统计表）
      epay index.php → 我们 Dashboard.vue [mock]

【订单管理】
  ✅  订单列表查询（筛选/分页）  epay order.php → Orders.vue [真接口 GET /api/admin/orders]
  🎨  订单详情 / 子订单
  🎨  API退款 / 手动退款
  🎨  冻结 / 解冻订单
  🎨  重新通知 / 补单
  🎨  删除订单 / 批量操作
  🎨  退款金额/退款订单统计
      —— 列表已真，写操作(退款/冻结/补单)全 mock。

【结算管理】(C2 结算域 2026-07-21 后端切真接口)
  ✅  结算列表（账号/姓名/商户号/方式/状态/批次筛选）  epay slist.php → Settle.vue [真接口]
        —— pay_settle 表 + GET /api/admin/settles(筛选/分页) + Settle.vue 接真数据。
  ✅  生成结算批次（收当前所有待结算→正在结算）  epay ajax_settle create_batch
        —— POST /api/admin/settle/batch，事务收批 status 0→2 + 建 pay_batch，无待结算记录时拦截。
  ✅  批次一键完成（正在结算→已完成）  epay complete_batch
        —— POST /api/admin/settle/batch/:batch/complete，批次内 status 2→1 + 写完成时间。
  ✅  单条状态变更（待结算/正在结算/已完成/失败/删除退回）  epay setSettleStatus
        —— PUT /api/admin/settles/:id/status。status=4 删除并把结算金额退回商户余额(事务+流水)。
           真跑验证：删除 money=200 记录→uid=1 余额 1739.80→1939.80 精确+200。
  ✅  结算批次列表  epay settle.php → GET /api/admin/settle/batches
  🎨  真实转账打款 / 批量转账（调支付宝/微信转账接口）—— 归代付域(C3)，本次未做
  🎨  结算打款文件导出（网商/支付宝批量模板）—— 归后续，本次未做
  🎨  支付密码校验（结算/退款二次密码）—— 归后续
        —— 计费规则(手续费率/门槛/封顶封底)现为 service 常量，待建 config 域迁移(见优化待办#5)。

【商户管理】
  ✅  商户列表查询  epay ulist.php → Merchants.vue [真接口 GET /api/admin/merchants]
  🎨  添加 / 编辑商户  epay uset.php
  🎨  余额充值 / 扣除
  🎨  查看/重置商户密钥、生成 RSA
  🎨  用户组列表/新增/删除  epay glist/gedit.php → Groups.vue
  🎨  用户组购买价配置       epay group.php
  🎨  资金明细流水            epay record.php → Records.vue
  🎨  支付统计（按方式/通道，可导出）  epay ustat.php → MerchantStats.vue
  🎨  支付用户统计            epay buyerstat(菜单有文件缺) → BuyerStats.vue
  🎨  授权域名审核/管理        epay domain.php → Domains.vue
  🎨  邀请码增删/清空          epay invitecode.php → InviteCodes.vue
      —— 列表已真，其余全 mock。

【支付接口】
  ✅  支付通道 CRUD/费率/成功率  epay pay_channel.php → Channels.vue [列表+写操作全真接口]
  🎨  支付方式 启用/配置          epay pay_type.php → PayTypes.vue
  🎨  支付插件列表                epay pay_plugin.php → Plugins.vue
  🎨  通道轮询规则                epay pay_roll.php → Rolls.vue
  🎨  微信公众号/小程序账号        epay pay_weixin.php → Wechat.vue
  🎨  企业微信账号                epay pay_wework.php → Wework.vue
  🎨  微信客服支付(H5跳转)         epay set_wxkf.php → WxkfSettings.vue
  🎨  获取用户标识(openid工具)     epay gettoken.php → GetToken.vue
  🎨  支付相关配置                epay set.php?mod=pay → PaySettings.vue

【财务管理 / 代付】
  🎨  手动转账付款(单笔)  epay transfer_add.php → Transfer.vue [mock]
  🎨  付款记录/撤销/改状态/退款/凭证  epay transfer.php → TransferRecords.vue
  🎨  批量转账付款        epay transfer_batch.php
  🎨  计费/账单            → Billing.vue（我们细化项）

【风控管理】
  🎨  风控拦截记录        epay risk.php → Risk.vue [mock]
  🎨  黑名单增删查         epay blacklist.php → Blacklist.vue
  🎨  风控检测配置(频率/人机验证)  epay set.php?mod=risk → RiskSettings.vue

【分账】
  🎨  分账接收方规则 CRUD/启停  epay ps_receiver.php → ProfitSharing.vue [mock]
  🎨  分账记录/发起/解冻/回退/改额  epay ps_order.php

【系统设置（epay set.php 16 个 mod）】
  🎨  网站信息配置 site        → Settings.vue [mock]
  🎨  注册相关(开放/审核/付费)  set?mod=site → RegSettings.vue
  🎨  支付相关配置 pay          → PaySettings.vue（见支付接口）
  🎨  风控检测 risk            → RiskSettings.vue（见风控）
  🎨  结算规则 settle          → SettleSettings.vue（见结算）
  🎨  转账付款配置 transfer     → (Transfer相关)
  🎨  快捷登录 oauth(QQ/微信/支付宝)  → OAuthSettings.vue
  🎨  消息提醒 notice(公众号/邮件)    → NoticeSettings.vue
  🎨  实名认证 certificate(支付宝/腾讯云/阿里云OCR)  → CertSettings.vue
  🎨  邮箱与短信 mail(SMTP/短信模板)  → MailSettings.vue
  🎨  网站公告 gonggao          → Announcements.vue
  🎨  首页模板 template          → TemplateSettings.vue
  🎨  网站LOGO upimg            → (Settings内)
  🎨  IP获取方式 iptype          → (OtherSettings)
  🎨  计划任务 cron             → CronSettings.vue（后端见第三部分）
  🎨  中转代理 proxy            → OtherSettings.vue
  🎨  管理员账号/支付密码 account → (Settings/权限)

【其他功能】
  🎨  导出订单        epay export.php+download.php → Orders.vue 导出抽屉[前端真导出]
        —— 前端已实现按 时间范围/商户/方式/通道/状态 过滤 → 生成 CSV(18列,带BOM,Excel不乱码)下载。
           2026-07-21 验证：选 0721 命中并导出 3 条。局限：只导当前已加载(≤100条)，
           真·全量后端流式导出留待阶段C(见 优化待办.txt)。
  🎨  登录日志        epay log.php → Logs.vue / OpLogs.vue
  🎨  数据清理        epay clean.php → Clean.vue
  🎨  文章管理        epay article.php → Articles.vue
  🎨  单点登录(后台免密进商户中心)  epay sso.php [未做]

【权限（我们扩展，epay 是单管理员）】
  ➖🎨  多管理员 Admins.vue / 角色 Roles.vue / 操作日志 OpLogs.vue
        —— epay 只有单管理员单级权限，我们做了 RBAC 雏形(前端 mock)。


================================================================
第五部分 商户中心（epay user 37 文件 → 我们 MerchantLayout /m）
================================================================
说明：UI 全部建好(21 视图)。D1 登录态已切真接口(密码/密钥登录、路由守卫、退出、
      merchant scope 隔离)，其余查询/资料页仍 mock，按 D2/D3 逐步接后端。

【登录 / 注册 / 找回】(D1 登录态 2026-07-21 切真接口)
  ✅  密码登录        epay login.php(type=1) → MerchantLogin.vue [真接口]
        —— POST /api/merchant/login type=1(账号=邮箱/手机, bcrypt 校验密码)。
           认证语义对齐 epay(双模式+type纠偏+状态守卫),但登录态载体用项目统一 JWT(scope=merchant),
           不移植 epay 的 RC4 cookie。真跑:密码登录成功签发 token+返回商户信息。
  ✅  密钥登录        epay login.php(type=0) → [真接口]
        —— type=0(账号=商户ID, 明文比对通信密钥 AppKey,对齐 epay key 比对);
           纯数字≤6位账号自动纠偏为密钥登录。真跑:uid+AppKey 登录成功。
        —— 前端:merchantAuth store + lib/api/merchantAuth + client 独立 merchant_token key(与admin隔离)
           + 路由守卫 /m 分组保护(requiresMerchant,未登录拦到 /m/login 带 redirect)+ 退出清态
           + 401 分端跳登录 + 顶栏商户信息去硬编码改从 store 读。
           真跑全通过:密码/密钥登录、未登录拦截、redirect回跳、退出清态、scope隔离(merchant token 访问 admin 接口 403)。
  ⬜  极验滑动验证码   epay 有；我们商户登录页未做（代码核实无验证码组件，
                      极验仅出现在后台 RegSettings 的配置项，与登录页无关）
  🎨  QQ 快捷登录/扫码  epay connect.php/qrlogin.php
  🎨  微信快捷登录      epay wxlogin.php
  🎨  支付宝快捷登录     epay oauth.php
  🎨  注册(邀请码/须知/验证码)  epay reg.php → MerchantReg.vue
  🎨  找回密码          epay findpwd.php → MerchantFindpwd.vue
  🎨  强制完善资料       epay completeinfo.php → MerchantComplete.vue

【用户中心首页】(D2 2026-07-21 切真接口)
  ✅  工作台仪表盘(余额/订单/结算/今昨收入/费率表/曲线)  epay index.php → MerchantHome.vue [真接口]
        —— GET /api/merchant/dashboard 聚合：商户信息(余额/状态/组名)+提醒横幅(未实名/未绑密保/未设密码)
           +四数据卡(余额/已结算/订单总数/今日订单)+通道费率表+结算趋势图。统计口径对齐 epay getcount
           (已支付订单数/今昨收入按 money 求和/已结算按 realmoney 求和)。真跑:余额¥1889.66等真实值渲染。

【个人资料 / 账户】(D3 2026-07-21 核心块切真接口)
  ✅  API信息(商户ID/MD5密钥)  epay userinfo.php?mod=api → MerchantApi.vue [真接口]
        —— GET /api/merchant/apikey 返回商户ID+MD5密钥(AppKey)+接口地址。
  ✅  重置MD5密钥         epay resetKey → POST /api/merchant/apikey/reset [真接口]
        —— 随机32位hex,原密钥立即失效。真跑:重置生成新密钥。
  ⬜/🎨  生成RSA对 / keytype 签名模式  —— 属 V2 协议(V2 后端未实现),前端保留UI标注"V2待开放",不接后端(不造悬空)
  ✅  修改资料(结算账号/联系方式/扣费模式)  epay editinfo.php → MerchantProfile.vue [真接口]
        —— PUT /api/merchant/profile:settle_id/account/username/email/qq/url/mode(仅Merchant模型已有字段)。
           真跑:改QQ生效。密保手机改绑(需验证码)/keylogin/refund/transfer/remain_money(模型无字段) 保留UI待后续。
  ✅  修改登录密码       epay userinfo.php?mod=account → PUT /api/merchant/password [真接口]
        —— bcrypt,已设密码校验旧密码,新密码≥6位。真跑:改密后旧密码失效/新密码可登录/错误旧密码拒绝。
  🎨  消息通知/语音播报设置  —— Merchant 模型无 msgconfig 字段,待通知域(前端保留UI)
  🎨  实名认证(个人/企业,支付宝人脸/微信)  epay certificate.php → MerchantCertificate.vue
  🎨  保证金(查看/充值/退还)  epay deposit.php → MerchantDeposit.vue

【查询】(D2 2026-07-21 切真接口)
  ✅  订单记录查询/退款/重新通知  epay order.php → MerchantOrders.vue [真接口]
        —— GET /api/merchant/orders(uid强制过滤+分页) + 退款 POST /order/refund(全额:状态转2+
           逆向退回已入账分成+退款流水+幂等,余额层退款渠道侧真退待凭证) + 重新通知 POST /order/notify
           (复用商户通知重发)。真跑:退款余额精确-0.14、重复退款拒绝、重新通知成功。缺:部分退款/支付密码校验。
  ✅  结算记录                    epay settle.php → MerchantSettle.vue [真接口]
        —— GET /api/merchant/settles(uid过滤+状态筛选+分页),商户端专用 View(number金额/boolean auto/failReason)。
  ✅  资金明细                    epay record.php → MerchantRecords.vue [真接口]
        —— 新建 RecordRepo + GET /api/merchant/records(uid过滤+类型/单号筛选+分页)。
  ✅  申请提现                    epay apply.php → MerchantApply.vue [真接口]
        —— GET /apply/info(可提余额D+1扣当日已收/手续费/次数) + POST /apply(校验金额/门槛/可提/权限/次数
           → 复用结算域 CreateWithDebit 生成待结算单+扣款)。真跑:提现50余额精确-50、超额/低门槛正确拒绝。
  🎨  余额充值                    epay recharge.php → MerchantRecharge.vue
  🎨  购买会员(升级费率组)         epay groupbuy.php → MerchantGroupbuy.vue
  🎨  授权域名管理                epay domain.php → MerchantDomains.vue
  ⬜  交易投诉(complain_open)      epay complain.php → [我们未做,epay源码也缺此文件]

【其他 / 增值】
  🎨  代付管理/记录             epay transfer.php → MerchantTransfer.vue
  🎨  新增代付(支付宝/微信/银行卡)  epay transfer_add.php
  🎨  聚合收款码                 epay onecode.php → MerchantOnecode.vue
  🎨  邀请返现(链接/比例/统计)     epay invite.php → MerchantInvite.vue
  🎨  使用说明                    epay help.php → MerchantHelp.vue
  🎨  站内信/通知抽屉             （我们扩展）
  ⬜  测试支付(1元)              epay test.php → [未做]


================================================================
第六部分 官网 / 前端视觉（epay template + paypage → 我们 SiteLayout）
================================================================
说明：epay 有 11 套首页模板 + 独立收银台视觉。我们做了 1 套现代 SaaS 官网 + CMS。

  🎨  官网首页(营销落地页)   epay template/default+index1~10 → SiteHome.vue [CMS可编辑]
  🎨  开发者文档            epay doc → ClassicDocs.vue [CMS结构化]
  🎨  服务协议 / 关于我们    → ClassicAgreement/About.vue
  🎨  支付成功 / 支付失败页   epay includes/pages → ClassicPayok/Payerr.vue
  ➖  官网内容 CMS(首页文案/文档后台可编辑)  —— 我们扩展,epay 无独立 CMS
  🎨  收银台聚合页视觉(cashier PC/H5)  epay cashier.php+main12.css → CashierMock.vue [真实二维码已接]
        —— A3 现代风收银台中间页(订单信息+扫码区+模拟支付)。
        —— 2026-07-21 接真实二维码：下单 dispatch 回填 pay_type/qr_code 到订单，CashierView 输出，
           收银台前端用 qrcode 库把真实渠道 code_url/qr_code 渲染成二维码图 + 3秒轮询查单(status=1 跳成功页)；
           mock 渠道仍走"模拟支付"按钮(plugin=mock 判别)。真跑：mock 下单 qr_code 落库+CashierView 返回正常。
           缺：H5/移动端唤起 App、微信内 JSSDK 调起。
  ⬜  扫码中间页(微信绿/其他蓝)  epay includes/pages → [未做]
  ⬜  个人收款码收银台(数字键盘仿微信)  epay paypage/ → [未做]
  ⏸  epay 11 套首页模板多选  —— 我们做 1 套精品,不追求 11 套


================================================================
第七部分 ➖ 我们独有 / epay 没有的（SaaS 化方向）
================================================================
说明：这些是我们把"支付系统"升级为"多租户 SaaS 平台"的扩展，epay 本身没有。
      不算精仿缺口，是加分项。全部前端 mock。

  ➖🎨  SaaS 控制台 /console —— 平台方多租户管理（epay 无此层）
  ➖🎨    租户管理(套餐/期限/权限/通道/配额)  Console.vue
  ➖🎨    租户套餐定价  ConsolePlans.vue
  ➖🎨    租户计费      ConsoleBilling.vue
  ➖🎨    分站总览      ConsoleOverview.vue
  ➖🎨    操作审计      ConsoleLogs.vue
  ➖⬜    P2剩余: 免密登录分站/下发公告/白标定制/独立域名SSL  [未做]
  ➖🎨  后台 RBAC 多管理员+角色（epay 单管理员）


================================================================
★ 结论与差距总结
================================================================
1. 精仿"界面层"≈90% 完成：admin 后台、商户中心、官网 UI 基本全对齐 epay 并超出
   (多了 SaaS 控制台 + RBAC + CMS)。这是我们最扎实的部分。

2. 精仿"能力层"≈5% 完成：epay 真正的价值(收单/渠道/回调/结算/代付/分账/定时)
   在我们这里几乎全是 mock。后端只有 登录+订单列表+商户列表 3 个只读真接口。

3. 最大且唯一的关键缺口 = 收单主链(第一/二/三部分)：
   下单→选通道→插件下单→收银台→回调验签→改单→入账→通知商户→结算/代付/分账→定时。
   这条链一通，0538pay 才从"支付管理界面"变成"支付系统"。

4. 补齐顺序(对应 开发进度总览.txt 的阶段 A~E)：
   A 收单核心MVP(mock渠道跑通全链路) → B 真实渠道3~5家 → C 后台业务域切真接口
   → D 商户中心接后端 → E 定时任务+收尾。
   横向(后台页面切真接口)价值不掉、可后置；纵向(收单链路)是未验证的最大风险,先啃。

5. 不追求 1:1 的项：epay 11 套首页模板(我们 1 套精品)、51 插件全做(按资质分级)、
   单管理员权限(我们升级为 RBAC)、投诉功能(epay 源码本身该文件缺失,核对后再定)。
